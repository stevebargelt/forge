**Last session ended 2026-07-10 (operator-directed 4-item sequential queue; STOPPED at item 2/4 per the queue's stop-conditions).**

**Where we left off:** The overnight queue (reviewed-tip equality → notify wedges → FG-512 provenance → canonical resolvePhasePrimary) stopped mid-item-2. FG-516 (unattended-wedge notifications) is implemented, tested, and CI-green, but SIX review-loops each surfaced new findings (~11 reviewer rounds) — classic churn per the operator's stop instruction, so no merge (never a merge exception) and the queue halted. A decision_needed milestone was pushed.

**Picked up next:**
1. FG-516 resolution — operator call first: continue fix rounds, or accept-and-batch the residuals. State: branch fix/fg-516-notify-unattended-wedges pushed at 460792b (all fixer commits pushed, nothing stranded), PR #95 OPEN/unmerged, CI green at every pushed head. The two OPEN findings from the last loop (run-review-loop-fg-516-bb0e8f round 2, both fail-safe over-notification races, NOT silence): (a) campaign-pause dedupe is run-scoped — a retry that clears runId then re-parks on a NEW run re-pushes for the same campaign+item (milestone.ts:154); (b) some park paths emit even when tryTransitionCampaign didn't commit — a concurrent operator pause can still trigger a push (executor.ts:849). Loop 6's round-1 fixer commit 460792b already fixed the two round-1 findings (drive/startRun parks now gate on the transition boolean; blockerKind persisted for gate/drive parks) — the round-2 pair above is what remains. Recommended path: ONE targeted engineer fixer on the same run (run-fg-516-notify-unattended-wedges-26e6f2) for the pair, push, one fresh loop; if THAT fails, batch remaining fail-safe findings into a ticket-body disposition (FG-510 precedent worked once already this session — reviewers respect recorded dispositions) and re-loop once.
2. Queue items 3+4, NOT started, specs verbatim in the operator's queue message: ITEM 3 = FG-512 (stamp dispatchSource on ALL runner-minted rows — primaries, fanout children, reds, on_reject recovery, request-changes replacements; delete legacy ambiguity refusal only for stamped rows; test: workflow step literally named "task" retryable once stamped, legacy unstamped still refuses). ITEM 4 = file-then-fix canonical resolvePhasePrimary (three disagreeing rules: ready-queue.ts:80-82 any-complete, inputs.ts:37-40 latest-regardless-of-status, runNext.ts:1401-1404 latest-complete; single latest-COMPLETE helper, parity tests + mixed-shape regressions, update FG-477 body; do NOT absorb broader FG-477, isRunSettled untouched).
3. FG-513 (split from FG-508): reviewer profile rotation claude→codex between loop rounds + model_error resilience. FG-508 itself was descoped to the CLI-pin fix and CLOSED.

**External state to remember:** PR #95 open awaiting FG-516 resolution. BACKLOG CAVEAT: FG-517/FG-518 ticket files and FG-516's body edits (scope resolution + disposition) exist only in PR #95's branch commits (3f977bd, d72aa17) — on main they won't appear in `forge backlog list` until #95 merges; don't re-file them, and read their bodies from the branch if needed. Review-loop trails: ~/.forge/runs/run-review-loop-fg-516-{f5041d,da6afc,8c1b22,ea61aa,bb0e8f}/review-loop.md (+ run-review-loop-fg-514-{3a23bf,665075}). Ops-check baseline: the 7 pre-existing orphaned_work_may_persist rows + FG-425 stuck run (unchanged, cleanable whenever); plus one NEW benign artifact: task-test-engineer-c96170 on the FG-516 run shows failed (harness killed the backgrounded wrapper mid-dispatch; recovered via forge retry → task-task-852b9c complete — the FG-507 retry path's first live dogfood, it worked). Deliberately deferred from the queue by the operator: FG-508-part-2 image-rebuild smoke work and the merge-exception ledger.

**Decisions worth not relitigating (this session):**
- FG-508 descoped to the codex-CLI-pin fix and closed on AC1 evidence (operator call: the ticket was over-bundled); resilience scope lives in FG-513.
- FG-514 landed WITH the review-loop's own trust check strengthened to remote-head EQUALITY + bounded fetch; an offline review-loop is now not-closeable BY DESIGN. Merged f135f95 + docs d494694. The loop verified its own merge ("is the head of @{u}").
- FG-515 pattern lesson (cost a loop round): do NOT pre-file follow-ups for findings a review-loop fixer might absorb — file after the loop settles.
- FG-516 zero-runs campaign-pause corner deferred to FG-517 (campaign-scoped channel = new notify machinery; the item's own scope guard forbade it — AC1/AC4 conflict recorded in the ticket body). FG-518 = resume-probe park stale-context (fail-safe deferral, disposition in ticket body).
- Queue stop discipline held: no merge without closeable/closeout_guidance_only, no PR #87-style exception under any framing.

**Shipped (for reference):**
- FG-514 (PR #93, merge f135f95): reviewed-tip trust = equality with freshly-fetched remote head; remote_ahead + diverged outcomes; fail-closed fetch; run-note persistence on all exit paths. + docs PR #94 (merge d494694): seed/CLAUDE.md merge-authorization rules, campaign-runner-plan supersession, review-loop skill trust section.
- FG-515 (same merge): diverged outcome + note persistence — closed.
- FG-508 closed (descoped, PR #88 evidence b03a3f5). FG-513/FG-516-ticket/FG-517/FG-518 filed. FG-516 code on PR #95 (unmerged): 19 park sites + ops live-mode notify, deduped, fail-safe, ~15 new tests across 4 fix rounds.
