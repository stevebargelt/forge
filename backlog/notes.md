**Last session ended 2026-07-26.**

**Where we left off:** FG-559 merged (`7549b9f`) and deliberately LEFT OPEN on one acceptance line — the production agent image has never been observed exiting 122. The session's last direction was a stop rule the operator set after five review-loop rounds of diminishing-returns edge cases: audit every container-starting path once, fix only concrete gaps, run CI once, do NOT restart review-loop, merge on green. That rule was executed and the loop is closed — do not reopen it.

**Picked up next:**
1. **Close FG-559** — the only remaining AC is the container-side probe. Requires `docker/build.sh` (the image is STALE; `agent-entrypoint.sh` and `forge-test.sh` both changed), then ONE dispatch against a deliberately broken linked worktree confirming the container exits 122 and forge records `container.git_unavailable`. **Blocked on docker.io being reachable** — see external state. Everything else in the ticket is evidenced and merged.
2. **FG-345 needs an OPERATOR DECISION before any implementation** — folded in from the closed FG-621: does host-side commit become authoritative under worktree mode (and the stale contract at `worktree-lifecycle.ts:231-242` gets corrected), or do agents get a writable git? This is the critical path for the worktree default-on flip and it is not the orchestrator's call. Do not start FG-345 implementation without it.
3. **FG-356** — orphan worktree cleanup. Ready NOW: its dependency FG-351 is done, and it needs nothing from the FG-345 decision, so it can run in parallel. Concrete evidence it matters: `git worktree list` currently shows a leftover `/private/tmp/forge-fg583-review.r8yO2f` from an old FG-583 review.
4. **FG-623** — measured ~2% flake on the REQUIRED `test` check (1ms knife-edge against a live clock). Small, and it removes noise from every future merge gate.

**External state to remember:**
- **docker.io is UNREACHABLE from this host.** `docker/build.sh` hung 60+ minutes on `load metadata for docker.io/library/ubuntu:22.04`, and a bare `docker pull ubuntu:22.04` timed out silently. Docker's daemon is healthy (28.4.0) — this is network, not docker. `forge notify` also reported `ntfy failed — network: fetch failed`. This is the sole blocker on closing FG-559.
- **The agent image is STALE** and `forge-dev upgrade`'s release check says so. `docker/agent-entrypoint.sh` (the new 122 git probe) and `docker/forge-test.sh` both changed this session and are NOT in the built image. Any agent dispatched right now runs the OLD entrypoint. The host-side mount still works (that's `spawn.ts`, live from linked source) — only the container-side probe is missing.
- **FG-625's root cause is UNKNOWN and the ticket says so — do not re-guess it.** Two separate diagnoses were made and both are recorded as wrong: (a) that the release integration tier's clean-checkout refusal caused the stop — that was inferred from the fixer's own separate `forge-test --integration` run, not from review-loop's verifier; (b) that the defect is specific to `--local-extended` — `verifyWithReuse()` runs `scriptsForVerification()` on ANY dirty tree, and this project's derived gate list is `["npm run test:all","npm run test:extended"]`, so extended runs without the flag. Verified 2026-07-26. The real blocker is that the post-fixer verifier DISCARDS `verification.steps`, so `verification_failed` carries no failing step, command, tier, or output. FG-625's decided direction is evidence-first: preserve and surface that result BEFORE changing any behavior. Do not commit-before-verify, do not skip `test:extended`, do not touch FG-575's assertion.
- **There are TWO distinct local-verification paths in review-loop; do not conflate them.** Round-entry `verifyWithReuse()` (dirty tree → FULL tier incl. `test:extended`), and post-fixer pre-commit `fix()` → `runVerify(localFallbackScripts())` (FAST tier: typecheck + test). The operator message `fix left uncommitted (verification failed)` comes from the second. Re-running those two fast-tier commands on the resulting FG-559 tree passes (typecheck exit 0; `npm run --silent test` exit 0, 2785/2785) — which disproves the release-tier story but does NOT explain the historical stop.
- **FG-617 stays closed.** Its refusal for an operator manually running the release tier is a separate accepted limitation. An amendment folding it into FG-625 was written and then reverted; do not re-add it unless new path-2 evidence names that tier.
- `~/code/forge-stable` (worktree pinned to an older main) and the disposable clones `~/code/forge-fg559`, `~/code/forge-fg624` still exist. Delete when no longer wanted.
- `forge-dev` is NOT on PATH despite being declared in `package.json` bin — `./bin/forge-dev` works from the checkout. Stale npm link; CLAUDE.md tells the orchestrator to run `forge-dev upgrade`, which fails as written.

**Decisions worth not relitigating:**
- **The red READ surface widening was ACCEPTED by the operator.** A whole-`.git` read-only mount lets a red read every branch, tag, reflog and sibling task branch. The isolation contract covers panel findings and the blue transcript, which are not stored in `.git`, and reds need history to review honestly. `docs/invariants.md` #9 is amended to say this. **Do NOT build a red-only `--single-branch` clone path.**
- **The 6-minute CI shard budget does NOT move.** Raising `timeout-minutes` was proposed and rejected — the gate exists so suite-time growth gets addressed, not absorbed. FG-624 fixed it by balancing (5 shards, ~1.4x spread) with the budget untouched.
- **No more review-loop on FG-559.** Five passes, nine findings; the closeout audit then found all four promises already held on both project-mounting paths, with one stale docs sentence left. The audit's structural result: only TWO argv builders bind a project dir, both guarded at a chokepoint. Sampling was the wrong method; enumeration took one pass.
- **Two FG-559 gaps were REPORTED, not fixed, on purpose.** R1: reconcile cannot record `container.git_unavailable` for a 122 provisioner death in the host-crash window — unrecordable (`--rm`, no retained exit code), would need stderr-parsing machinery on a five-condition conjunction. R2: a `.git` that is a file but not a `gitdir:` pointer — not reachable in practice. Both documented in the ticket.
- **Ticket-filing discipline was corrected mid-session.** The operator audited 8 tickets and dispositioned 4 of mine as fold/close/demote. Before filing: a fail-safe limitation that fails LOUDLY gets accepted, a deferred design option folds into its parent, a latent issue on another ticket's surface folds there, and a small verified correction just gets fixed. Memory written: `feedback_dont_file_low_substance_tickets`.
- **FG-622 stays an idea, not a story.** Relative `gitdir:` pointers (submodules, git ≥2.48) fail loudly at the probe, which satisfies the safety requirement; pinned by a characterization test.

**Shipped (for reference):**
- **FG-559** — read-only parent `.git` bind for linked-worktree mounts; unconditional refusal (no env bypass); exit-122 sentinel classified on agent, provisioner and reconcile paths; structural verification of the pointer before mounting (SECURITY: a project-controlled `.git` could otherwise bind-mount any host path into every container); 28 tests. `7549b9f`, PR #161. **Still OPEN on AC 4.**
- **FG-624** — duration-aware integration sharding. Worst CI shard 6m10s timeout → 3m50s, budget unchanged. `4ad20e8`, PR #162.
- **FG-615** — dropped the stale "strip the `closed:` frontmatter" reopen instruction; `moveTicket` already strips it. `58b6b68`.
- Dispositioned: FG-611→FG-600, FG-616→FG-608, FG-621→FG-345 (all closed); FG-617, FG-620 closed; FG-622 demoted to idea.
- Filed with real evidence: **FG-623** (2% flake, measured 1ms margin), **FG-625** (review-loop deadlock, five hand-commits of evidence).
