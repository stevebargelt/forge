**Last updated 2026-07-11 (late evening), mid-queue.**

**Where things stand:** The operator restarted the autonomous queue (FG-531 → FG-533 → FG-535 → FG-536) with durable tmux process ownership mandatory (CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1 confirmed armed). Queue item 1 is DONE: FG-531 shipped and closed — the killed fixer's partial edit was recovered intact (snapshot at ~/.forge/sigterm-probe/fg531-fixer-partial-93be42.diff, byte-identical to committed a177db4), review-loop closeable, PR #107 merged (670ebaf), AC walk done, ticket in done/. The stranded evidence rows (task-engineer-93be42, run-review-loop-fg-531-dd1eda) were swept to terminal states by reconcile (FG-531's own sweep code) and recorded.

**In flight:** FG-533 (queue item 2) is implemented on PR #108, branch fix/fg-533-pre-container-crash-window: reconcile's pre-container sweep (retryable pre_container_crash; five guards preserving the invoke/manual/fanout-parent/live-container/live-lock exemptions), the FG-533 matrix pin flipped to a passing recovery assertion (no live pins remain), plus the review-loop round-1 fix extending the ops read-surface (reconcile-candidate.ts / forge show) to SEE the shape. Docs reconciled (concepts.md pre-container section, how-to-testing.md, FORGE-DEC-009 scope note). Remaining: clean closeable review-loop re-run, merge on both CI checks green at the exact head, AC walk, close.

**Queue remainder:** FG-535 (durable tmux launcher + operator guidance; attribution is operationally sufficient — si_pid 17165 captured the Claude harness directly, and the plain-iTerm kill proves Supacode is not necessary; do NOT spend the run on the old Supacode A/B matrix) then FG-536 (cause-agnostic detached agent survivability; drop any claim that user-away/lock/display-off is the established cause; prove submitting-CLI death doesn't kill the container and reconcile finalizes through the normal validation contract without weakening trust gates).

**External state:** ~/.forge/sigterm-probe/ holds the sentinel binary, the 18:13 smoking-gun sentinel.log (si_pid 17165 = the old session's own harness), and the FG-531 fixer snapshot; nothing running. Dashboard restart still pending at operator convenience (predates FG-521 server-side change).
