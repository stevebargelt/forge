**CHECKPOINT 2026-07-19 — FG-561 campaign 3 of 4 shipped; FG-563 handed off (do NOT auto-start; operator wants it taken fresh).** Clean state preserved at `main` = `f4a98b2`.

**SHIPPED + CLOSED this session:**
- **FG-555** (merged `21af80c`) — R3/R4 launched-workload provenance + `--require-control-toolchain` (Option A pinned-PATH trust; merge authorized despite review-loop non-closeable). 7 rounds.
- **FG-552** (merged `2e95b0c`) — blocking `forge launch wait` completion primitive (atomic records, bounded-retry-to-terminal, no false-completion, F32 publication windows, launcher-liveness `starting` bound, exhaustive exit-record validation). 11 rounds. PRD-locked: a live tmux owner is NOT terminal evidence.
- **FG-562** (merged `fbb1fc8`) — durable continuation-claim PRIMITIVE (new additive `continuations` table + phase-bound CAS + `adoptOrClaimDispatch`/deterministic dispatch_key + `continuationsInDispatch` replay + `continuation_stale_observations` audit; BD-3 claim-only-from-`ready`). Operator DECISION A: primitive-only. + **defect repair** (merged `f4a98b2`, PR #141): immutable dispatch identity (CAS-guarded run/task ids in recordDispatchResult/markAdvanced) + complete phase-bound adoption identity (adoptOrClaimDispatch validates the full 5-tuple continuationId+sourceLaunchId+consumerKind+currentPhase+canonical nextAction before returning `adopt`).

**NEXT WORK — FG-563 (Slice 4, interactive orchestrator adoption). LAST slice. Take fresh; needs operator's direct involvement. Three items are the crisp next work:**
1. **OQ-2 — MUST be answered with HOST EVIDENCE, not assumed.** Name the *production* session adapter that converts a `forge launch wait` completion into a Claude Code **session wake** when `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1` disables ordinary Bash background dispatch, and its restart behavior. Do NOT assume `<task-notification>` can be emitted externally. The FG-553 OQ-2 feasibility spike was the prerequisite probe; FG-563 owns the DECISION. **If no external mechanism can wake this session, the Monitor-polling workaround is RETAINED as the named fallback adapter (FG-563 decides its fate) rather than retired.** This is the load-bearing gate — resolve it before touching the orchestrator policy.
2. **BD-3 authoritative-read requirement (added as binding AC to FG-563 this session, commit `db11231`).** The orchestrator adapter MUST obtain the launch disposition from the canonical launch reader (`readLaunch`/`classifyExit`) IMMEDIATELY before observe/claim and pass that exact observation into the FG-562 primitive — no caller-fabricated/stale terminal disposition. (FG-562 records a caller-supplied observation; it does NOT establish authority — that is this slice's job. Regression must prove a fabricated/stale status cannot advance a phase.)
3. **F17 receipt-keyed check-before-spawn (added as binding AC to FG-563, `db11231`).** Before dispatching physical work, the adapter MUST do the deterministic `dispatch_key`-keyed lookup (`adoptOrClaimDispatch`) and ADOPT an existing dispatch rather than spawn a duplicate; on controller restart, use `continuationsInDispatch` + the receipt to adopt the original in-flight run, never a second. (FG-564 carries the same consumer-enforcement AC for campaigns.)

Also in FG-563 scope: F19 (happy-path job outrunning any estimate → no model wake) + F22 (a routine chain reaching its next decision with the operator sending ZERO messages — the live meatgeekv2 stall) observed RED against the current fixed-estimate/Monitor behavior; ScheduleWakeup demoted to a low-frequency lost-signal watchdog (OQ-3, health bound not a duration guess) with durable "signal missed" evidence; OQ-4 adoption-cancel (cancel observation vs work as distinct commands/audit); retire-or-retain the Monitor workaround. **Propagation: `seeds/orchestrator-template.md` (the canonical policy — orchestrator-DIRECT surface, NOT the engineer's) + the CLAUDE.md marker block re-rendered via `forge-dev upgrade` (NOT `forge upgrade`).** Recommend an OQ-2 feasibility + architecture pass FIRST (like FG-562's).

**FILED THIS SESSION — FG-594** (dashboard defect, for the dashboard orchestrator): the dashboard's in-progress/stale verification badge (`dashboard/src/queries.ts`) flags a `review_loop.verification_started` event with no matching `verification_finished` as in-flight WITHOUT checking the owning run's status — so a killed/abandoned review-loop shows `stale · waiting-on-ci` for 24h. Reproduced live via `run-review-loop-fg-552-eaf24c` (I killed it mid-CI-wait during a CI-hang recovery); reconciled by inserting the matching `verification_finished` event. Fix: exclude verifications whose run is terminal (abandoned/failed/complete), preserving FG-487 active-run stale flagging.

**PROCESS LESSONS (this session):**
- Container agents (`forge invoke`) sometimes emit `Co-Authored-By: Claude` trailers; squash-merge scrubs `main` (verified clean on 21af80c/2e95b0c/fbb1fc8/f4a98b2). Review-loop fixer commits are clean. Worth a ticket: fix agent container git identity.
- Do NOT commit `backlog/` edits into a feature branch (collides with the main handoff commit; hit on FG-555 #138).
- Salvaging a crashed `forge invoke` (143): watch CI ACTIVELY incl. duration — a salvaged self-unvalidated fix once hung CI 40+ min (a blocking wait test that never terminated). Every blocking-wait test must be bounded.
- When the review-loop reviewer persistently flags something the ticket/PRD settles (scope/design), disposition/advance-over or escalate — don't oscillate (FG-552 rl9 live-owner policy; FG-562 F17-scope → operator DECISION A).
- Killing a review-loop launch leaves its run row + open verification event dangling — abandon the run (`forge cancel <run> --abandon-run`) AND close the verification (or it shows stale on the dashboard, FG-594).

**External state:** `~/code/forge-agent-work` = writable clone, on `main` at `f4a98b2` clean. Live control checkout `~/code/forge` on `main` at `f4a98b2` clean. Branch protection: both `test` + `test-extended`. INTERIM dispatch override (multi-minute forge cmds via `forge launch run` + condition-driven Monitor observer). Do NOT mutate meatgeekv2 state. Terminal `forge-launch-fg55x/fg56x-*` tmux panes swept at checkpoint.
