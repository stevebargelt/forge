**Last session ended 2026-07-11 (late evening).**

**Where we left off:** Ran the operator's controlled SIGTERM experiment (plain iTerm, normal Bash run_in_background, no tmux/detach, SUPACODE_*/ZMX_* confirmed absent) over the strict 4-item queue. STOPPED at item 3/4 on the primary stop condition: a confirmed unsolicited kill of harness background task b9cr8t82y (review-loop FG-531) at 19:10:57 PDT — fixer container forge-task-engineer-93be42 exit 143, OOMKilled false, no TaskStop, killed mid-tool-call. Same backgrounded pattern had completed 3x earlier in the session. Proves Supacode is NOT NECESSARY for the failure; sender for this specific event unproven (no sentinel was armed in this session). Full forensics in FG-535's "SECOND CONFIRMED KILL" section; decision_needed milestone pushed. CAUTION: two live orchestrator sessions share this working tree — the other session overwrote this notes block once tonight and previously committed onto my feature branch (cherry-picked to main as 695ce87); verify notes/tree state before trusting either.

**Picked up next:**
1. FG-531 — finish and land. PR #107 open on branch fix/fg-531-awaiting-red-crash-window (2 commits pushed; code + docs complete; full matrix 1234/1234 locally, both FG-530-A cells flipped). Its review-loop round 1 returned needs_fix with ONE real finding: src/v2/fg530-crash-worktree.worktree.test.ts (the WORKTREE lane) still asserts the old FG-530-A wedge shape. The killed fixer's partial edit is UNCOMMITTED in the tree (M fg530-crash-worktree.worktree.test.ts; snapshot at ~/.forge/sigterm-probe/fg531-fixer-partial-93be42.diff) — finish that edit, run npm run test:worktree, re-run review-loop (--since abee0bf), merge on closeable + both CI checks green at the exact head, close with the AC walk. Also: DB row task-engineer-93be42 in run-review-loop-fg-531-dd1eda is deliberately left stranded `running` as kill evidence — recover it only after the operator has seen it (it is also, ironically, a live FG-533-adjacent shape).
2. FG-533 — the queue's item 4, not started (pre-container running wedge; preserve the invoke/manual exemption; flip the last matrix todo pin). Only proceed when the operator restarts the queue.
3. Non-ticket thread — operator decision pending (decision_needed pushed): next controlled comparison (recommend Supacode-with-hooks-disabled A/B, with a sentinel re-armed INSIDE the session under test so si_pid capture covers the killer), and FG-535/FG-536 reconciliation (recommend FG-536 drop the unproven user-away/lock CAUSE claim and reframe as cause-agnostic survivability-by-construction; tonight's killed fixer is its direct motivating casualty).

**External state to remember:**
- Working tree parked on fix/fg-531-awaiting-red-crash-window with the fixer's partial edit uncommitted — do NOT discard or stash-drop it.
- ~/.forge/sigterm-probe/ holds: sentinel binary + the 18:13 smoking-gun sentinel.log (si_pid 17165 = the OLD session's own harness), and fg531-fixer-partial-93be42.diff. Nothing running.
- agent-dev-worker image CURRENT; forge.db carries the FG-523 migration (backup: forge.db.backup-pre-fg523-20260710-213303). Dashboard (PID 21748) still predates FG-521's server change — restart at convenience.
- Upstream refs: anthropics/claude-code #76249 / #72851 / #68625 / #25188; host runs 2.1.207. Operator may post tonight's forensics upstream.

**Decisions worth not relitigating:**
- SIGTERM sender attribution: proven for the 18:13 event (si_pid=17165, that session's own harness); tonight's 19:10 kill matches the signature but is deliberately NOT attributed (no sentinel armed). Don't conflate the two claims.
- FG-513 root cause is settled: no rotation mechanism — per-dispatch model-policy re-read + mid-loop operator file edit (proven from task rows' resolvedBy + policy mtime). Reviewer now pinned per loop; one same-round model_error retry on the defaults path; operator --review-profile retries on itself.
- FG-531 recovery design is settled: fail-safe landings reusing orphaned_needs_finalize / fanout_wave_orphaned, run-lock liveness probe, dead-red settlement; verdict-adoption from reconcile deliberately REJECTED (FG-477 drift). No new status.
- FG-530 pin discipline: fixed bugs flip to passing assertions and lose their KNOWN_FAILURES pin (FG-530-B done, FG-530-A done pending #107 merge); only FG-533's pin remains.
- Standing: FG-530 exhaustive coverage boundary; FG-535/536 split; forge-test byte-compare; the 2026-07-11 failed engineer rows are audit residue.

**Shipped (for reference):** FG-532 (PR #105, merge 09ac1ea — gate reject preserves the rejected task's result; FG-530-B cell flipped) · FG-513 (PR #106, merge 56c0d07 — reviewer profile pinned per loop + bounded same-round model_error retry; new review_loop.reviewer_model_error_retry event; SCHEMA-CONTRACT/concepts/how-to-model-policy updated) · FG-531 code+docs on PR #107 (UNMERGED) · FG-535 evidence commits on main (695ce87 cherry-pick, edeab85 second-kill forensics).
