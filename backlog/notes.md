**Last session ended 2026-07-09.**

**Where we left off:** Cleared the entire handoff friction list plus two same-day operator-filed items — FG-497 (PR #71), FG-491 (PRs #72+#73, incl. one operator-caught reopen), FG-494 (PR #74), FG-501 (PR #75) — all merged, closed, pushed; session ended clean immediately after the FG-501 closeout. Also closed 11 stale orchestrator runs at session start (5 were live sessions mistakenly cancelled and restored via direct sqlite — see memory never-cancel-orchestrator-runs-by-age) and reaped an orphaned review-loop fixer (run-review-loop-fg-491-10eaf5) whose detached container had authored real tests that later shipped in PR #73.

**Picked up next:**
1. FG-487 (dashboard host-side-work visibility) — re-scope FIRST: the deterministic gate moved to visible CI checks and FG-501 made review-loop verification mostly a CI wait, but review-loop rounds + the new CI-wait phase are still dashboard-invisible (this session the operator asked "is something running?" during an invisible wait — that's the remaining unmooted half).
2. FG-492 (container disappearance diagnostics) — fresh evidence: three engineer containers were killed mid-task this session (FG-501's first engineer died with empty result.json + 439 lines on disk; two review-loops died in the harness kill window, one orphaning a live fixer). The kill-window pattern is now well characterized: long attached host phases die at ~10-12min; FG-501 shrinks exposure but container-death causal evidence is still not preserved.
3. FG-402 / FG-395 (attention inbox / campaign view) or FG-425 (per-projectDir gate locking) — normal backlog order; operator's call.

**External state to remember:** Pre-existing stuck run run-fg-425-e1dd27 still wants `forge ops repair` (operator call, unchanged). Ops-check orphan rows task-engineer-67e458/6609d6/0fd451 + cb3534 were FG-491 evidence; the ticket is CLOSED now, so they are cleanable whenever the operator wants (verify nothing else references them first). Branch protection unchanged (test + test-extended required; enforce_admins off).

**Decisions worth not relitigating:**
- Review-loop verification now WAITS on in-flight CI (FG-501): default 30s poll / 20min timeout (FORGE_CI_POLL_SECONDS / FORGE_CI_WAIT_TIMEOUT_SECONDS); failing check stops with NO local run; local fallback only on unavailable/timeout and runs the FAST tier only (--local-extended restores). Policy block + concepts.md + how-to-testing.md + test-suite-timing reconciled. Launch loops IMMEDIATELY after push now — waiting for CI first is obsolete.
- Evidence semantics unchanged on purpose: pending/failed CI never covers; failed wins over unavailable (a provider hiccup can't mask a sibling red); describeGateEvidence names EVERY verified check + URL.
- persistence watchdog (FG-491): only clear annotation shapes (empty/dash/paren remainder after the path token) count as path claims; prose NEVER proves persistence; all-unparseable fails closed. Operator's prose-first-word finding forced the reopen — reopen-don't-followup applied.
- FG-494: dispatched=actual delivery outcome + dispatchError on event/CLI (review-loop round-1 catch). Residual low, deliberately unfiled: multi-provider aggregate dispatched can't distinguish partial from total failure (single-provider setups unaffected).
- ntfy titles: RFC 2047 at >0x7F (also fixes latent Latin-1 mojibake); live-validated to the operator's topic.
- Killed review-loop protocol: check for a surviving detached fixer container + open task rows on the loop's run (this session's orphan authored shippable work); forge cancel the stale rows after salvage.
- Unfiled by discipline: review-loop kill-window deaths (mitigated by FG-501; refile only if it recurs post-FG-501), forge-test stale /tmp/forge-work cache (cost one agent a debug cycle — watch for recurrence), empty /project/node_modules in fresh containers (every agent this session ran npm install first; pre-existing, frequent, maybe worth a ticket if it keeps taxing agents).

**Shipped (for reference):** FG-497 (PR #71 — invoke task off argv/E2BIG + pi @file delivery + argv-size guard; ADR+proof harness reconciled). FG-491 (PR #72 annotated-claim normalization; PR #73 prose-claim rejection after operator-caught reopen; runNext-seam tests from the salvaged orphan fixer). FG-494 (PR #74 — RFC 2047 ntfy titles + delivery-outcome audit). FG-501 (PR #75 — review-loop waits for in-flight CI; live-proven twice in its own closeout). User-filed FG-501 committed same session. 11 stale orchestrator runs closed (pixtron/constellation/forge-site/harebrained-apps live ones restored after mistaken cancel; forge/pocket/older duplicates + orphaned 10eaf5 abandoned).
