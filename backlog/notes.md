**Last session ended 2026-07-08.**

**Where we left off:** Operator-directed autonomous batch fully landed — campaign campaign-4e43b64871d3 COMPLETE/all_shipped (FG-485/488/489/490, PRs #60–#63), plus a same-session reopen/re-close of FG-490 (PR #64) after the operator caught a HIGH in the shipped --json guidance. Last operator signals: invisible host-side work is bad and needs fixing (FG-487 carries the evidence + minimum bar) but is explicitly NOT scheduled next; redundant suite runs are waste (process changed: no pre-loop test:all — the review-loop's verification is the gate).

**Picked up next:**
1. FG-493 + FG-494 as a small quick-lane batch — both fresh, both directly harden the autonomous loop (review-loop accepting red-schema verdicts instead of false reviewer_failed; ntfy pushes surviving non-Latin-1 titles). Evidence and AC are in the tickets.
2. FG-477 slice 1 — file the task-lineage-classifier slice as its own ticket off the plan pasted in the FG-477 body (do not start the whole evaluator). Note the FG-485 liveness probe is a new slice-7 call site (addendum in ticket).
3. FG-474 (CI) — now carries tonight's numbers (~10 full local suite runs) and the fix shape: CI as required merge check, review-loop consumes covering verification evidence instead of re-running, evidence rows from CI/reconcile-capture. Operator pain twice this session.
4. FG-487 (host-verification dashboard visibility) — evidence-loaded from three live operator collisions; scheduling is the operator's call, ask before starting.

**External state to remember:** pre-existing stuck run run-fg-425-e1dd27 still wants `forge ops repair` (operator call). Ops-check orphan warnings on task-engineer-67e458/6609d6/0fd451 + the FG-491 evidence rows (task-engineer-7bc36b/889edb/07fe60/cb3534) are PRESERVED EVIDENCE — do not clean up or retry. Operator is actively working a bedrock/STS issue on the work laptop and edited FG-435 twice mid-session (both preserved: dbdf0e0, 52f10be) — expect more live edits to that ticket from other sessions.

**Decisions worth not relitigating:**
- FG-485 was DISPROVED as filed (event stream + two live repros: resume already re-drove; the bug was evidence-first ordering + a misleading refusal message). Ticket reframed/retitled at closeout — don't resurrect the "resume can't re-drive" narrative.
- FG-490 startRun-throw parks failed/blocked/infrastructure ON PURPOSE (review round-2 proved the as-filed non-terminal park was unrecoverable); recovery is retry-then-resume, and the --json guidance says so per shape. Don't "fix" it back to awaiting_gate.
- No pre-loop test:all (operator directive) — launch the review-loop directly; its round-1 verification is the deterministic gate. Post-merge evidence run only where a host_verifications row is needed (or reuse an identical-tree run, noted in journal D25).
- Run ALL long forge dispatch detached (nohup+disown) — tracked background tasks were externally terminated 3× (initiator unestablished, D15). Monitor durable artifacts (log terminal lines, result.json existence), never pgrep process names (two false signals: self-matching monitors, Codex argv pollution).
- Do NOT touch the repo during a live review-loop — including `forge backlog edit` (an FG-474 edit was eaten by the loop's tree restore and re-applied).
- Agent-filed backlog tickets are a channel violation even when content is good (FG-492 kept on merit); seed-contract fix is a deferred note, not filed.
- Deferred low-severity review residue lives in journal D24/D25 (notes/autonomous-decisions-2026-07-07b.md), deliberately unfiled — don't refile without new evidence.

**Shipped (for reference):** FG-485 (PR #60, dc7d725 — liveness-first resume reattach + truthful messaging + regression tests). FG-488 (PR #61, 2f910b6 — drive-loop no-progress bound → recovery_needed). FG-489 (PR #62, 6e8f4d1 — forge campaign retry verb; DB-edit guidance removed). FG-490 (PR #63, 1f8ced8 — drive catch-and-park + drive_error event; reopened, PR #64, b1d9c09 — per-shape --json guidance; re-closed 77f3252). Filed: FG-491 (watchdog false positive), FG-492 (container-death evidence, agent-filed/kept), FG-493 (loop verdict vocabulary), FG-494 (ntfy unicode drop). Campaign all_shipped with host-verification rows + done-audit passes per item. Docs reconciled per item by the maintainer. Journal: notes/autonomous-decisions-2026-07-07b.md D1–D25 (host-local).
