**Last session ended 2026-07-17.**

**Where we left off:** The operator authorized an autonomous FG-571 run and then drove it hard through **four read-only red-security audits** — each one commissioned because the *previous* mechanism turned out to be exploitable, not because a reviewer asked. FG-571 is **merged (`2f80496`, PR #124) and closed** with a line-by-line AC-to-evidence walk. The session's last acts were two record corrections the operator caught: the **F32 label collision** (fixed, `917c6f8`) and the **FG-140 cleanup warning** (`61d76a1`). `main` is clean and pushed; nothing is mid-flight. **FG-572 is deliberately stopped, awaiting explicit operator go.**

**Picked up next:**
1. **NON-TICKET, DO FIRST — checkpoint `~/code/forge-dashboard`.** It holds **4 commits on branch `dashboard-redesign` that exist on NO remote and nowhere else on disk**, incl. `defde2d checkpoint: recover dashboard plan-limit work after crash`. **`git status` says CLEAN and the branch has no upstream, so `git log @{u}..HEAD` errors and any naive check prints "0 unpushed"** — I nearly believed my own. Verify with `git ls-remote --heads origin dashboard-redesign` (empty) and `git branch -r --contains <sha>` (zero). Fix: `git push -u origin dashboard-redesign` from that dir. **Do not delete or relocate that path until this is done** — FG-140's cleanup TODO says it's safe to delete and that is now FALSE (warning recorded on the ticket).
2. **FG-572 (FG-553 Child 5: installed-surface compatibility) — STOPPED pending operator go.** Scope is a **read-only architecture/propagation pass ONLY**: no implementation, no child tickets, and **do NOT pick the dashboard product boundary** (bundled vs separately versioned vs intentionally unavailable) — that is reserved to the operator. Stable `forge dashboard` still refuses in release mode until it's decided, and **FG-561 is not complete while that's true**.
3. **FG-575** — pre-existing FG-569 test defects, filed not fixed: `release.integration.test.ts` **commits `source snapshot` into the real checkout** (bit me 3× this session; recover with `git reset --soft HEAD~1`) and asserts an un-canonicalized `/var` path (macOS-red/Linux-green). Sibling of **FG-556**; consider a shared canonicalizing assert helper so the class stops recurring. **FG-557**, **FG-559**, **FG-558** remain the other standing false-alarm/docs tickets.

**External state to remember:**
- **`~/code/forge-fg571` is the standalone writer-agent clone** for this campaign (never hand a writable main to a writer agent). Its `fg571-atomic-promote-rollback` branch is merged; the clone is on disk and reusable.
- **`~/code/forge-dashboard` is NOT the old #137 leftover** — it's a second clone of the forge repo carrying unpushed work. See item 1.
- **Promotion was NEVER activated on this host, by design.** Real `~/.forge/current`, `~/.forge/releases`, `~/.forge/interpreters` do not exist; machine-wide `forge` still resolves to `~/code/forge/bin/forge` (live checkout, `0.1.0`). Promoting is the operator's explicit `release build` → `promote` → `install-shim`.
- **`~/.forge/runtimes` is the PROVIDER RUNTIME REGISTRY** (`claude-oauth.yml`, `codex-subscription.yml`, `pi-*.yml`), read by `doctor.ts` filtering `.yml`. The FG-553 plan originally put the interpreter store there — collision; it's now `~/.forge/interpreters`. Don't put anything else in `runtimes/`.
- Untracked `docs/research/competitive/pinecone-forge-assessment.md` is the operator's research doc — never `git add -A` at repo root without looking.

**Decisions worth not relitigating:**
- **THE THREAT BOUNDARY IS SETTLED** (plan §4b + FG-571 ticket). Protected: untrusted candidates, parser ambiguity, hostile ambient env (F29), crashes, concurrent Forge ops. **Honest limit: a principal able to rewrite `$FORGE_HOME` owns the account anyway** — it already owns `~/.zshrc`, the checkout, the shim, and the validator. The 4th audit's 3 HIGH all required exactly that and are dispositioned out-of-scope. **`chmod` is an accident barrier, not a security boundary against its owner.** Stronger protection needs a separate trust domain (another OS principal / root-owned storage / hardware signing) — **deliberately NOT filed as a follow-up**; it's an optional product direction, not missing closure. Do not re-audit this.
- **The shim NEVER parses the release manifest.** It reads a forge-authored canonical execution descriptor *inside* the immutable unit. A POSIX-sh reader diverged from `JSON.parse` on duplicate keys → a candidate that PASSED validation exec'd `/tmp/attacker-node`. Teaching sh to match JSON.parse is unbounded machinery; the invariant moved instead.
- **The descriptor ships INSIDE the staged unit, never as a sibling of `current`.** My sibling-record proposal was rejected by the operator: a record swap + pointer swap is not atomic *as a pair* and just relocates the window. Same reasoning made `(current, previous)` one swap.
- **Interpreter identity is the full SHA-256 of the bytes** + version/ABI/platform/arch. `process.version`+ABI never pinned content (a rebuilt/patched Node collides), and a 64-bit truncation "for a legible pathname" made the immutable key collidable.
- **Selection evidence is the BYTES, never the pathname.** Store residency / path equality / id / existence / permissions / comments are NOT provenance. `lstat` alone cannot close it. Promote-by-id and rollback use durable content-bound evidence; **materializing promote-by-id would be the *laundering* version of the bug** (copying planted bytes into a unit forge then vouches for).
- **Canonical F32 is FG-552's** ("a reader arrives during meta-record publication", PRD:543). FG-571's local label is **FG571-RI**. Don't renumber canonical F32.
- **`npm link` is no longer the install path** and README/quick-start/work-laptop-setup/how-to-use-forge-across-projects say so. Design records that predate the split carry supersession banners **at the top of the document** — a mid-file marker is invisible to someone landing on line 90.

**Operational lessons (worth keeping):**
- **The whole-corpus grep must be FILE-level, not line-level, and must enumerate every changed premise.** I under-grepped **four times**, each differently: only the phrasing I remembered; against the wrong repo (main, lacking the branch's fixes); piped through `head` (silently truncated at 10 — the same truncation that hid `runtime-store.ts` from another check); and never including `backlog/`. A document-level banner is invisible to a per-line grep — ask "does every file carrying the stale claim carry a correction?"
- **Never trust a quiet round as evidence the code is clean.** I told the operator "code clean for 4 rounds"; the next loop found two real code defects. Docs-only rounds meant the reviewer was working the corpus, not that the code surface was exhausted.
- **The review-loop's local fallback runs the UNIT tier only, and its fixer commits are never pushed** — so a fix can be committed RED in the integration tier and neither the loop nor CI sees it. Happened twice (`5e74366`, `ab88b59`). Always host-verify the loop's own fixes.
- **A bounded audit beats iterative loop rounds when findings keep landing one-per-round.** Each audit found 3–6 real HIGHs where the loop was finding one; the loop's rounds were also re-finding what an audit would enumerate.
- **`forge launch run` + a Monitor on the launch record** drove every dispatch. I broke the rule once (`&`-backgrounded an invoke, rationalized as "short-lived") — killed and relaunched. `run_in_background`/`&` gets SIGTERM-swept.
- Mutants must prove **exploitability** (a filesystem marker showing attacker code RAN), not merely that the fixed version refuses.

**Shipped (for reference):**
- **FG-571** — FG-553 Child 4: atomic promote/rollback + PATH shim + env-sanitization + fail-closed identity (merged `2f80496`, PR #124; closed `e0a3132`). 147/147 across six integration suites on macOS; `test:all` 2420+79. **14 confirmed HIGH across 4 audits — 11 closed with exploit-proving mutants, 3 dispositioned as same-principal tampering.** Beyond original AC: `(current, previous)` one-swap; content-addressed interpreter identity; bytes-not-pathname selection evidence. Creates the stable/dev split (`bin/forge-dev` is new).
- **FG-575** — filed (not fixed): pre-existing FG-569 test defects.
- Record corrections: F32→FG571-RI (`917c6f8`); FG-140 delete-warning (`61d76a1`); plan/epic/PRD reconciled to the merge (`20c2a60`, `eb65439`).
