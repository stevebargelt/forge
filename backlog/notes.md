**Last session ended 2026-07-19.**

**Where we left off:** Autonomous FG-561 campaign drove FG-555 → FG-552 → FG-562 end-to-end (3 of 4 slices shipped) plus a bounded FG-562 defect repair; operator then chose to CHECKPOINT (option B) and take FG-563 fresh rather than start it this session. Clean state preserved at `main` = `f4a98b2` (code) / `46fdff2` (notes).

**Picked up next:** FG-563 is the LAST campaign slice (interactive orchestrator adoption) — take it FRESH; it rewrites the orchestrator's OWN policy and is gated on an unresolved feasibility question. Ordered moves:
1. **Resolve OQ-2 FIRST (the load-bearing gate) — answer with HOST EVIDENCE, do not assume.** Name the *production* session adapter that turns a `forge launch wait` completion into a Claude Code **session wake** when `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1` disables Bash background dispatch (do NOT assume `<task-notification>` can be emitted externally). If NO external mechanism can wake this session, the Monitor-polling workaround is **RETAINED as the named fallback adapter**, not retired. Recommend an OQ-2 feasibility + architecture pass before touching any policy. (The FG-553 OQ-2 spike was the prerequisite probe; FG-563 owns the decision.)
2. **BD-3 authoritative-read (binding AC already on FG-563, commit `db11231`):** the adapter MUST read the launch disposition from `readLaunch`/`classifyExit` immediately before observe/claim and pass that exact observation into the FG-562 primitive — a fabricated/stale status must not advance a phase (regression required). FG-562 only RECORDS a caller-supplied observation; establishing authority is THIS slice's job.
3. **F17 receipt-keyed check-before-spawn (binding AC on FG-563 + FG-564, `db11231`):** before dispatching physical work, do the deterministic `dispatch_key` lookup (`adoptOrClaimDispatch`) and ADOPT an existing dispatch rather than spawn a duplicate; on restart, use `continuationsInDispatch` + the receipt to adopt the original run.
4. Then the rest of FG-563: F19 (happy-path outrunning any estimate → no model wake) + F22 (a chain reaching its next decision with the operator sending ZERO messages — the live meatgeekv2 stall) observed RED against current fixed-estimate/Monitor behavior; demote ScheduleWakeup to a low-frequency lost-signal watchdog (OQ-3, health bound + durable "signal missed" evidence); OQ-4 adoption-cancel; retire-or-retain the Monitor workaround. Propagate to `seeds/orchestrator-template.md` (orchestrator-DIRECT surface — NOT the engineer's) + the CLAUDE.md marker block via `forge-dev upgrade` (NOT `forge upgrade`).

**Non-ticket threads (not my backlog items):** FG-594 (filed this session) is a DASHBOARD defect handed to the dashboard orchestrator — do not pick it up here. FG-564 (campaign adoption) and FG-565 (closeout) follow FG-563.

**External state to remember:** `~/code/forge-agent-work` is the writable implementation clone (on `main` at `f4a98b2`, clean). Live control checkout `~/code/forge` on `main` at `46fdff2`, clean. Branch protection on `main` requires BOTH `test` + `test-extended`; a clean review-loop (or explicit operator merge authorization) + both green authorizes the orchestrator to merge; backlog/notes commits go straight to main. INTERIM dispatch override: every multi-minute forge command via `forge launch run` + a condition-driven Monitor observer (never a fixed wakeup as primary). Do NOT mutate/reconcile meatgeekv2 state. Container agents (`forge invoke`) sometimes emit `Co-Authored-By: Claude` trailers — squash-merge scrubs `main` (verify), but worth a ticket to fix agent git identity. Do NOT commit `backlog/` edits into a feature branch (collides with the main handoff commit). Killing a review-loop launch leaves its run row AND its open `review_loop.verification_started` event dangling — `forge cancel <run> --abandon-run` AND close the verification event, or the dashboard shows it stale (that gap is FG-594).

**Decisions worth not relitigating:**
- **FG-562 = primitive-only (operator DECISION A).** BD-3/F17 are MET *at the primitive*; end-to-end consumer enforcement is FG-563/FG-564's job (added as their binding AC). Do not pull consumer adoption or a `readLaunch` coupling into FG-562, and do not re-dispute the reviewer's BD-3/F17 findings — they are valid DOWNSTREAM requirements with owners.
- **FG-552: a live tmux owner is NOT terminal evidence.** A persistently-invalid exit record + live owner stays `running` (bounded by the waiter `--timeout` → `wait_timeout`), never a fabricated terminal; only independent owner-gone evidence arms the bounded terminal. PRD-locked; do not reopen.
- **FG-555: Option A pinned-PATH-trust contract**, and merge was authorized despite the review-loop being non-`closeable` on the name-resolution-trust residual (operator decision). Don't re-debate the contract shape.
- When the review-loop reviewer persistently flags something the ticket/PRD settles, disposition/advance-over with rationale or escalate — do not oscillate (cost FG-552 rounds; resolved on FG-562 via escalation → DECISION A).

**Shipped (for reference):** FG-555 (`21af80c`) launched-workload R3/R4 provenance + `--require-control-toolchain` contract. FG-552 (`2e95b0c`) blocking `forge launch wait` completion primitive. FG-562 (`fbb1fc8`) durable continuation-claim primitive + defect repair (`f4a98b2`, PR #141 — immutable dispatch identity + complete phase-bound adoption identity). FG-594 filed (dashboard stale-verification badge, for the dashboard orchestrator).
