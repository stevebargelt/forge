**Last session ended 2026-07-23.**

**FG-583 SHIPPED & CLOSED** (FG-572 Child 5h) across two merges — `8272e5b` (PR #154: atomic seed generation + move-the-invariant) and `b0dd651` (PR #155: routing authority model + documented bootstrap + real-CLI acceptance test). Closed with a CORRECTED AC-evidence grid (7 original AC + 3 reopened-scope items, all met).

**FG-572 CLOSED** (umbrella — all 7 installed-surface children done). **FG-605 CLOSED** (absorbed — the whole routing authority model landed in FG-583, not just route preflight).

**The reopen (why the first close was premature):** operator review at `8272e5b` found the seed-generation invariant only HALF-held: (1) HIGH — every host-policy consumer still read the flat `~/.forge/routing-policy.yml` while docs/invariants.md claimed policy moves with the generation; (2) MEDIUM — the documented fresh install (install-seeds.sh flat copies, no generation) was non-dispatchable; (3) MEDIUM — the promoted-layout acceptance test used library calls, not the installed CLI. Reopened FG-583 + FG-572, encoded a re-close AC contract, and closed the WHOLE authority model per operator direction.

**Routing authority model (b0dd651):** host policy resolves from the anchored seed generation (flat only as a no-generation sentinel); dispatch consumers thread the held anchor; `forge route compile` (host) / `raci apply` are REFUSE-and-DIRECT (validate + install the RACI source but direct to `forge upgrade` for effect — no silent flat rewrite); the generation's routing-policy is manifest-verified (closed-set, like workflows/runtimes); the route decision is anchored across the preflight→dispatch boundary; no-generation fails closed by consumer class (dispatch refuses, operator reports the named state); routingGovernance passes. Architecture consult settled republish-vs-refuse toward REFUSE (a republish from an operator action would mix release-owned + operator-authored provenance in one manifest). Fresh install: DOCUMENTED bootstrap `build → promote → install shim → forge upgrade --skip-project → forge setup` (NOT install-seeds.sh publication — it runs pre-promotion = dev bytes; no promote-time two-pointer publish).

**Adversarial review earned its keep twice:** red-wide + red-security on the routing diff found 5 real gaps (incomplete refuse-and-direct incl raci apply + explicit-path; unverified generation policy; preflight/dispatch cross-generation; incomplete-vs-absent naming; a stale work-laptop-setup doc) — all fixed (`24596da`) with regression tests. A campaign-lease unit test flaked on CI (timing; passed 13/0 x3 on host, unrelated to the diff); cleared on rerun. Final CI green (test + test-extended).

**NEXT — FG-553 + FG-561 still UNBLOCKED but NOT closed.** All FG-561 slices done except FG-553 (Slice 1), whose six children (FG-567–572) are all closed. FG-553 has SUBSTANTIVE aggregate AC (BD-14 control-plane availability; "machine-wide blast radius ELIMINATED — documenting it does not close this ticket"; R1+R2 provenance) that require an aggregate-evidence AC walk across the children — NOT a children-are-done rubber-stamp. Once FG-553 closes, FG-561 (epic) closes. This is a dedicated reconciliation, deliberately not rushed.

**External state:** Writer clone ~/code/forge-agent-work on main synced to origin (`c692bc3`); branches feat/fg583-atomic-seed-generation + feat/fg583-routing-authority merged + deleted. Control checkout ~/code/forge main still has 4 UNPUSHED local commits (competitive-research docs + a working-plan backlog file) predating FG-583 — DIVERGED from origin, not fast-forwardable; operator should rebase them onto origin/main. `forge` runs npm-linked from the control checkout. NOTE: host test runs with `env -u FORGE_HOME` can leak into the real ~/.forge (a stray constraints/note.md appeared and was removed); isolate FORGE_HOME when running tests directly on the host.
