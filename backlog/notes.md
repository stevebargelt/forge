**Last session ended 2026-07-15.**

**Where we left off:** The whole session was **FG-569 (FG-553 Child 2: release closure + exec-not-spawn entry + immutable closure + honest R1/R2 provenance)**. It is **MERGED (squash `1b11f25`, PR #121) and CLOSED** with a durable line-by-line AC-vs-evidence walk committed to its record (`ac9485b`). `main` is at `ac9485b`, clean and pushed. What made this session long: the bounded review-loop ran **12 rounds** and the operator ran **two independent HIGH repros** — every finding was real, and all were fixed and host-verified by execution before merge. The very last exchange: the operator stopped me for spinning up a documentation-maintainer to reconcile the PRD/epic — that was a mis-route (see decisions). FG-573 reconciliation is still open and is mostly orchestrator/operator-owned, NOT agent work.

**Picked up next:**
1. **FG-573 (docs reconciliation) — do this correctly, by hand, split by ownership.** The FG-561 **epic** (`backlog/epics/…`) is BACKLOG → orchestrator authors it directly via `forge backlog`. The **PRD** (`docs/prds/durable-orchestration-continuation.md`) is the OPERATOR's product artifact — do NOT hand it to a documentation-maintainer; let the operator drive it or make a targeted edit for their review. Only `docs/concepts.md` (add the durable R1 `control:` record alongside the existing R2 `runtime.json`) is genuinely maintainer territory, and it's one small section. Content to land: R1 and R2 ARE now recorded (per-launch), the store open-path is additive-only (FG-568), the entry is exec-not-spawn — while keeping R3/R4 (FG-555), promotion (FG-571), the ABI floor (FG-570), and the deferred stable dashboard (FG-572) honestly open.
2. **FG-570 (Child 3: bounded ABI assertion)** — the next Slice-1 implementation child; depends on FG-569 (now landed). Still STOPPED pending operator go. Normalize/confirm its contract before dispatch.
3. **The standing test/infra false-alarm tickets** — FG-551 (agent image lacks tmux → 10 FG-535 launch tests hard-fail in every container), FG-556 (`/var` symlink canonicalization, macOS-host-only), FG-557 (wall-clock re-sync threshold), FG-559 (linked-worktree `.git` pointer). None block; together they'd make a host/agent test run trustworthy at a glance. FG-558 (dashboard PRD missing `awaiting_recovery`) is a small docs fix.

**External state to remember:**
- Untracked `docs/research/competitive/pinecone-forge-assessment.md` is the operator's own research doc — approved, left unchanged. Never `git add -A` at repo root without looking.
- The merged branch `fg569-release-closure` (squash-merged) can be deleted on GitHub/local at leisure.
- Branch protection on `main` requires BOTH `test` and `test-extended`; a clean review-loop + green CI authorizes the orchestrator to merge, but this session the operator held every merge decision explicitly.

**Decisions worth not relitigating:**
- **Resolution B (honest control-plane closure) is SETTLED.** The release bundles the small asset dirs supported control commands need (`seeds/`, `scripts/`, `docker/`); the **dashboard is a separate workspace and is NOT bundled** — `forge dashboard` refuses in release mode with a named nonzero error, deferred to FG-572 (Child 5). **FG-561 is NOT complete while stable `forge dashboard` from a release is unavailable** (operator caveat, recorded in FG-572).
- **Durable per-launch R1 is REQUIRED by the PRD (F30 "submitted and later inspected"), not scope creep.** A live `forge release provenance` assertion cannot satisfy later inspection. R1 is captured inside the submitting CLI, independent of R2, surfaced separately in `launch show` (human+JSON); old records show "not recorded", never manufactured.
- **A release derives its builder identity from its OWN manifest, never `.git` (a release has none) and never `--source`** — this is what lets a release build its successor (the successor-build HIGH blocker).
- **The manifest commit describes the shipped bytes BY CONSTRUCTION:** commit-bound content is materialized from `git archive <commit>`, closing the copy-window TOCTOU (a post-copy live recheck could not). `node_modules` stays lockfile-bound.
- **R2 releaseId is derived from the manifest, never the ambient `FORGE_RELEASE_ID`** (a caller could forge it). Malformed R2 records are omitted, never guessed.
- **Do NOT route backlog epics or the PRD to a documentation-maintainer.** Backlog (incl. `backlog/epics/`) is orchestrator-direct; the PRD is operator-owned. Operator corrected this at end of session.

**Operational lessons (worth keeping):**
- **Engineers self-committed several times with a meaningless `source snapshot` subject — amend to a descriptive message before pushing.** They also occasionally added an out-of-scope `allowScripts` block to `package.json` (a container package-manager side effect) — revert it.
- **CI `test-extended` is the real gate; host fast-tier (`test:all`) is not sufficient.** The immutability freeze broke `launch-r2.integration.test.ts`'s teardown (`rmSync` EACCES on a now-read-only release) — `test:all` doesn't run that file, so only CI `test-extended` caught it. Rely on CI for the extended tier.
- **Host `npm run test:extended` dies under tmux ("owner gone") — the integration tier kills process trees that reach the tmux pane running the suite.** Don't fight it; let CI run the extended tier.
- **`forge launch run` + a Monitor on the launch record drove the whole multi-round chain cleanly.** Watch durable launch state, never pgrep; host-verify every agent claim by execution (reproduce the repro), never trust the result JSON.

**Shipped (for reference):**
- **FG-569** — FG-553 Child 2: immutable release closure + manifest + exec-not-spawn entry + honest R1/R2 provenance (merged `1b11f25`, PR #121). Includes: lockfile→closure binding covering install-script packages; read-only-at-rest freeze; commit↔source binding via git-archive snapshot (TOCTOU-closed); successor-build (release builds its successor without `.git`); durable R1 + unspoofable R2 surfaced separately; control-plane asset bundling + dashboard release-mode refusal; POSIX-portable entry.
- Backlog reconciliations to `main`: FG-569 contract expanded (`0c488e7`), FG-572 owns deferred stable-dashboard + campaign caveat (`278e171`), FG-567 done-record superseded-note (`184f90e`), FG-573 filed for the PRD reconciliation.
