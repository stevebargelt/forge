**Last session ended 2026-06-29.**

**Where we left off:** Long Phase-3 session. Shipped the whole readiness→done-audit→shipping-reviewer chain plus a major review-tooling hardening detour. The user was an active reviewer — they found a real gap after almost every close (FG-413/416/383/415 each had a justified reopen), which motivated FG-417. Last action: deployed the shipping-reviewer seed. FG-384 is the last close; its docs were deferred to FG-388.

**Picked up next:**
1. **FG-388** — document the Reviewer Context Packet contract (now incl. the new `doneAudit` field) + the shipping-reviewer verdict vocabulary/fail-loud precondition. This is FG-384's explicitly deferred docs; closing it ends that thread. The reviewer is now reachable (mapping + packet wired), so FG-388's "when FG-384 wires the reviewer reachable" precondition is met.
2. **Wire shipping-reviewer into a default pipeline workflow (NON-TICKET thread — not yet filed).** The reviewer is fully built and reachable but DORMANT: no default workflow lists `shipping-reviewer` in its `reds`, so it never runs. Decide whether to add it (and to which workflow / at what authority + gate_on_verdict) or leave it opt-in per-project. File a ticket if you pursue it.
3. **FG-385** — risk-targeted reds planner (the remaining Phase-3 quality-gate piece; explicitly out of scope of FG-384). And **FG-386** — dashboard surface for readiness/done-audits.
4. **FG-414** — ops-check should detect active-run/all-terminal-tasks orphans + reconcile the `projects show` in-flight count with the dashboard (filed early this session after `forge projects show` reported a phantom "in-flight: 2").
5. **FG-410** — parallel-safe `updateCampaignItem` (carried over; low urgency, only needed before FG-396 parallel lanes).

**External state to remember:**
- TWO seed deploys done this session via `FORCE=1 scripts/install-seeds.sh` (the script defaults to `cp -n` no-clobber — FORCE=1 is REQUIRED to update existing installed seeds): the red-wide production-path-trace rubric (FG-417) and the shipping-reviewer rich-verdict contract (FG-384). Both live in `~/.forge/agents/`. install-seeds is machine-wide; only the intended files differed each time.
- A host-verification RECORDER does not exist (deliberately out of FG-383 scope). Until one ships, done-audit `host_verification` is always `unknown` for real items → `all_shipped`/done-audit `pass` is unreachable for real campaign items. Building that recorder (possibly alongside the shipping reviewer) is what unlocks a real `pass`.

**Decisions worth not relitigating:**
- **review-loop is now SAFE to use (FG-415).** It refuses to start on a dirty tree, scope-guards the fixer (touching `backlog/`/`docs/`/`learnings/`/`README*` → round reverted + `fixer_out_of_scope`), commits each accepted in-scope round itself (verify-then-commit), and feeds the reviewer a DYNAMIC diff (original + fixer commits since loop start) so a later round reviews the actual fix. Use it. The ONLY exception: do not use review-loop to review changes to review-loop itself — use a manual read-only red-wide there (it caught two guard bypasses FG-415's own reviews missed).
- **The recurring miss is now tooled (FG-417).** Reviewer briefs (review-loop `reviewTask` + red-wide seed) require a production-path consistency trace: source-of-truth → collector → evaluator/policy → state-transition/re-run → operator surface AND JSON → real-input tests. It caught a real tautological-fixture gap on FG-384's first dogfood. The underlying lesson: NEVER close a "surface/report/distinguish/gate" AC on "the evaluator/JSON supports it" — verify the real data path POPULATES it (FG-383 collector was hardcoded null), the human surface RENDERS it (FG-413), and no step reuses a STALE snapshot after mutation (FG-415 P1 / FG-416).
- **Shipping-reviewer verdict mapping (FG-384):** rich verdict maps onto red pass/fail/inconclusive via `mapShippingReviewerVerdict` — gate.ts UNTOUCHED (protected invariant). `ship`→pass; `ship_with_named_deferrals`→pass ONLY when every deferral has description+followUpTicketId else fail; `needs_fix`→fail; `needs_human`→inconclusive. Guardrail backstop: a `ship` over a fail/unknown done-audit with no named accepted_exception/covered_by_deferral is downgraded to fail. done-audit lives in the Reviewer Context Packet. Decided against a standalone state-machine path.
- **done-audit (FG-383)** is truthful-but-conservative BY DESIGN: missing required evidence → `unknown` (never pass); `container_verification` is informational and excluded from aggregation and never satisfies `host_verification`; deferral_linked requires the follow-up named IN the deferral section (not just any `related[]`).
- **Container env gotcha (reconfirmed several times):** agents run with empty container node_modules and will `npm install` in `/tmp` scratch — watch for stray `package.json`/lockfile tsx version bumps in their diff (revert them) and treat "needs npm install / forge-test failed on better-sqlite3" notes as container artifacts; always verify on the HOST.

**Shipped (for reference):** FG-413 (readiness preflight wired into campaign runner — reopened once for operator-surface consistency) · FG-416 (dependency-held resume message — operator loop fix) · FG-383 (mechanical done-audit evaluator + report wiring — reopened once to collect container evidence) · FG-415 (review-loop fixer scope guard + commit determinism + dynamic diff — reopened once for stale-diff + README* guard) · FG-417 (production-path consistency-trace reviewer rubric, both surfaces) · FG-384 (LLM Shipping Reviewer verdict mapping + done-audit in packet). Filed: FG-414, FG-415, FG-416. All pushed to origin; host `test:all` green (2330 + 27).
