**Last session ended 2026-07-09 (autonomous campaign session).**

**Where we left off:** Operator-ordered batch FG-487 → FG-492 → FG-362 → FG-365 → FG-366 executed via campaign-7a56519b2f3d (plan hash d36aa3e09b99…dda6ed, sequential; lanes: 2 full_feature, 1 docs_only, 2 quick_implementation — recorded at approval). ALL FIVE tickets shipped, merged, and closed: FG-487 (PR #77, 949eff0 — host-side verification visibility: eager review-loop run rows, attemptId-paired verification events, reconcile gate events, host_verifications dashboard views), FG-492 (PR #78, 5281ef8 — container causal evidence: no more --rm, capture-at-close, outcome-keyed retention, four distinguishable terminal states across show/status/ops check, ops reap-containers), FG-362 (PR #79, dfbae8c — docs/invariants.md), FG-365 (PR #80, 6c0bf92 — model policy once per dispatch wave), FG-366 (PR #81, 0df6fa9 — outer manifest runtime.name records the resolved runtime; consumer audit in the PR body). PR #82 (in flight at session end: FG-366's loop-fixer AC4 fast-tier test that stranded local-only, cherry-picked; plus the FG-502 ticket file that accidentally rode the branch) — merge it when CI is green, no review-loop needed (test+backlog only).

**Campaign state:** FG-362/365/366 items = shipped (via reconcile, two-pass pattern: first pass refuses lane_evidence_missing, second pass real-execs the gate and ships). FG-487/FG-492 items = failed/blocker=campaign_system despite shipped tickets — known runner gap, evidence in FG-502. The campaign is effectively DONE; abandon or leave as the operator prefers (items are terminal; do NOT re-dispatch).

**Filed this session:** FG-502 (consolidated, threshold: repeated pattern + blocks closeability) — review-loop fixer scope-guard reverts whole rounds over in-diff docs (6 occurrences), campaign_system items unrecoverable by reconcile after out-of-band delivery (2), stranded local fixer commits (1, FG-366). Everything else journal-only (notes/autonomous-session-2026-07-09.md · deferred review notes): campaign show "recovery needed" stale text during reattach; dashboard staleness cutoffs hardcoded vs env timeouts (conservative-direction only); red-security retention window (accepted + documented per pre-launch policy).

**Decisions worth not relitigating:**
- FG-492 retention: reap keys on TASK OUTCOME not container exit code (clean-exit result_missing retains); reap paths use docker rm -f -v or the DEC-019 anonymous shadow volume leaks; provisioner keeps --rm (FG-437).
- FG-487 events: start/finish pairing is by per-invocation attemptId, never latest-unmatched-by-key; reconcile gate events are runId-nullable payload-keyed; no new state tables (query-time derivation, stale-flagged not hidden).
- FG-366 = option 1 (audit cleared: consumers key on logFormat/kind, never .name).
- The build-fanout step-independence rule now includes TYPE independence — a step importing a sibling step's new exports fails isolated typecheck (request-changes'd twice this session; consolidate coupled contract+consumers into ONE step).
- project-meta hash test made collision-tolerant (single random pair legitimately collides in finite hue space — flaked red in CI).

**Operational lessons (all journaled):** engineer containers must NEVER background their validation then end_turn (task-build-0-b06fcc died $15 into FG-487 that way — work salvaged from disk per the verify-on-disk memory); after ANY argv/exec-shape change run the extended tier locally before pushing (my miss on FG-492 loop-6: CI test-extended went red on assertions the fast tier never runs); containerized fixers can corrupt dashboard/node_modules native binaries (npm rebuild better-sqlite3 fixes); review-loop merge gate = closeable + BOTH CI checks green at the exact head — it caught both the red test-extended and the flake.

**Picked up next (operator's call):**
1. Merge PR #82 when green (test + FG-502 ticket file only).
2. FG-502 — the review-loop/campaign friction ticket; highest-leverage platform fix from this session's evidence.
3. Normal backlog order: FG-402/FG-395 dashboard views, FG-425 gate locking (pre-existing stuck run run-fg-425-e1dd27 still wants forge ops repair — unchanged), FG-496 DB-backed backlog.

**External state:** ops-check orphan rows from before this session unchanged (FG-491/FG-501 evidence, cleanable). Branch protection unchanged (test + test-extended required). notes/ has per-loop logs for every launch this session (fg487-review-loop*.log, fg492-*.log, fg36[56]-*.log, campaign-7a56519b2f3d.log).

**Addendum (same session, post-batch):** Operator review finding on shipped FG-492 → FG-503 filed, fixed, SHIPPED (PRs #83 + #84, merged 1b96936 + a12a13a, closed). Success-path reap failures now recorded as container.reap_failed on all four call sites, and ops reap-containers candidacy is DISK-TRUTH-driven (docker ps -a vs terminal task rows) so even the crash-window leak (complete→reap gap, zero events) is sweepable; ops check raises the leak incident. SECOND stranded review-loop fixer commit occurred (FG-503 loop-3 round-1, recovered via PR #84) — FG-502 updated: the pass verdict evaluated a tip that was never pushed, so the loop's unpushed-tip warning is now a correctness item, and the standing orchestrator discipline is: verify the loop-reviewed tip sha == the PR head before merging. Workspace clean, main = a12a13a.

**Addendum 2 (same session):** Operator's second review pass on FG-503 → FG-504 filed, fixed, SHIPPED (PR #85, merged e0c42c9, closed). `container.reaped` resolution events recorded by the sweeper on confirmed-gone outcomes (rm success AND not_found); `container_reap_failed` incidents in ops check are suppressed by a later resolution for the same container (detector stays pure-DB); the completed-leak output wording splits honestly ("now swept" only for confirmed-gone, "NOT confirmed gone — left for a later sweep" on rm error), asserted through the real CLI in live mode plus a full fail→incident→repair→cleared lifecycle test. Loop passed round 2; tip-sync verified before merge (the new standing check). Workspace clean, main = 3de3254.
