**Session 2026-07-18/19 (autonomous FG-561 campaign, operator engaged intermittently).** GO: FG-555 → FG-552 → FG-562 → FG-563 end-to-end. Recovery governor in force.

**SHIPPED + CLOSED this session:**
- **FG-555** (merged `21af80c`, closed `1af8474`) — R3/R4 launched-workload provenance + `--require-control-toolchain` contract. Two operator decisions: **Option A pinned-PATH-trust contract**; **merge authorized despite review-loop non-closeable** (name-resolution trust = accepted, documented residual). 7 review rounds.
- **FG-552** (merged `2e95b0c`, closed `986bb13`) — blocking `forge launch wait` completion primitive: atomic exit+meta records (temp+rename), evidence-honest reader (empty/torn/XOR/integer/safe-integer all bounded-retry), reconciled owner_gone/unknown (never fabricated), minimal native-free observer (F33), subscribe-race close (BD-6), OQ-4 waiter-cancel, OQ-5 post-reboot, launcher-liveness `starting` bound. **11 review rounds** — every finding a real, distinct completion-semantics edge; the reviewer accepted all fixes. **Design ruling made & PRD-locked: a live tmux owner is NOT terminal evidence** — a persistently-invalid record + live owner stays `running` (bounded by the waiter `--timeout` → `wait_timeout`), never a fabricated terminal; only independent owner-gone evidence arms the bounded terminal. rl9 oscillated on this; I ruled per the PRD and the reviewer then accepted it (rl10/rl11 were doc/comment cleanup only).

**IN PROGRESS — NEXT: FG-562 (Slice 3 — durable continuation claim).** Branch off `2e95b0c` in `~/code/forge-agent-work`. **Involves an operator-AUTHORIZED schema/migration to shared `~/.forge/forge.db` + a `docs/SCHEMA-CONTRACT.md` obligation + concurrent-version handling.** Migration MUST be additive-only on the ordinary open path (FG-568 pattern; destructive changes go through quiesce-gated `forge store converge`) — the shared DB has machine-wide blast radius (every running forge re-runs migrations on next open). Back up before irreversible changes. Then FG-563 (interactive orchestrator adoption — do NOT auto-edit memories; propose one-line corrections per the ticket).

**KNOWN PROCESS ISSUES (this session):**
- **Container agents (`forge invoke engineer`/`documentation-maintainer`) emit `Co-Authored-By: Claude` trailers** on their commits (violates the no-AI-attribution force constraint). Squash-merge scrubs `main` (verified clean on 21af80c + 2e95b0c), but the branch commits carry it. **Worth a ticket: fix the agent container git identity/config so agents never write the trailer.** Review-loop fixer commits are clean; only direct `forge invoke` commits carry it.
- **Do NOT commit `backlog/` edits into a feature branch** — collides with the handoff commit on main (hit on FG-555 #138; resolved by aligning branch to main). Backlog/epic reconciliations that a reviewer flags: edit on the branch only if unavoidable, else on main.
- **Salvaging a crashed `forge invoke` (143) is fine — but WATCH CI ACTIVELY (duration included).** A salvaged self-unvalidated fix hung CI 40+ min (a blocking `forge launch wait` test on a signal launch that never classified terminal, from a signal-record regression). Every blocking-wait test must be bounded. CI is the authority for "unrelated/pre-existing" dirty-tree failures (fg571-*, launch-r2/FG-569-R2 refuse to build a release from a dirty scratch tree — green on clean CI).
- **Review-loop `--since <base-sha>`; fixer commits are local (unpushed) → the loop's verification for them falls back to local (fast tier) and the dashboard shows "stale · waiting-on-ci".** Normal, not a hang. `exited_ok` from the review-loop launch = PASS/closeable; `exited_error` = any non-pass stop.

**External state:** `~/code/forge-agent-work` = writable clone; live control checkout `~/code/forge` on `main` at `986bb13` clean. INTERIM dispatch override (all multi-minute forge cmds via `forge launch run` + condition-driven Monitor observer). Branch protection: both `test` + `test-extended`. Do NOT mutate meatgeekv2 state. Many terminal `forge-launch-fg552-*`/`fg555-*` tmux panes accumulated — sweep at session end.
