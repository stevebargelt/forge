**Last session ended 2026-07-19.**

**Where we left off:** FG-563 (Slice 4 — interactive orchestrator continuation-consumer adoption) SHIPPED and CLOSED — merged `9a3235d` (PR #143), backlog close `12dc847`, control checkout pulled so the completion-driven wait policy is now live. The campaign is one slice from done: FG-564 (campaign adoption) then FG-565 (closeout).

**Picked up next:**
1. **FG-564 — Slice 5, campaign-runner adoption.** Make the campaign executor consume the SAME completion primitive + terminal vocabulary FG-563 shipped: `forge launch wait` → the continuation-consumer claim/adopt path (`consumerKind: 'campaign'`). Reuse — do NOT build a second watcher/classifier/event-table (BD-10). It carries the identical binding BD-3/F17 consumer-enforcement AC as FG-563 (authoritative read before claim; receipt-keyed check-before-spawn; crash-recovery adopts the original run). **Its AC demands PRODUCTION-PATH proof** — real `runNext` + real publisher + real durable rows, truthful convergence of ALL FIVE state levels (task/run/campaign-item/campaign/publication) — a fixture-green suite does NOT satisfy it (that was FG-425's failure pattern). Preserve the four FG-425 constraints (shared `git_state` blocker lifetime, cancel-is-terminal-but-surface-landed-candidate, `CONVERGE_LIMIT=2`, no refusal over a `publishing` attempt).
2. **The campaign consumer needs its own stable controller identity.** FG-563 shipped a fail-closed resolver (`--owner` > `FORGE_CONTROLLER_ID` > `CLAUDE_CODE_SESSION_ID` > refuse, never hostname) in `src/cli/commands/continue.ts` (`resolveControllerOwner`). A campaign runner is a different controller kind — decide its durable identity source (it is not an interactive Claude session) so `renewClaim` fences correctly; do not let it fall back to a host-stable owner.
3. **FG-565 — closeout** after FG-564: cross-layer fault matrix, operator-visible recovery evidence, retire temp guidance, final reviewer maps evidence to every BD/matrix row. **FG-565 owns the PRD current-state-map final consistency pass** — reconcile `docs/prds/durable-orchestration-continuation.md` rows (Orchestrator adoption / FG-562 consumer-adoption / notification-delivery) to shipped, WITH merge shas. That reconcile was deliberately deferred from FG-563 (needed the merge sha).

**External state to remember:**
- `~/code/forge-agent-work` is the writable implementation clone (now on `main`, clean). Control checkout `~/code/forge` is the LIVE npm-linked control plane — dispatch every WRITING agent against the CLONE via `--project`, never the control checkout (FG-553 promotion is NOT in force on this host).
- **Memory flag (handoff obligation, NOT applied):** `feedback_schedulewakeup_not_sleep_for_waits` is not wrong but now INCOMPLETE — it describes the interim Monitor-polling-`forge launch show` form; the shipped canonical pattern is a disposable Monitor wrapping one blocking `forge launch wait <id>` → `forge continue`. Proposed one-line addition (operator applies): "Shipped FG-563: production pattern = disposable Monitor running one blocking `forge launch wait <id>` → `forge continue --continuation-id/--source-launch/--phase/--next-action` (fail-closed controller identity); ScheduleWakeup is only a health-bound lost-signal watchdog." No memory asserts the old duration-estimated-wakeup behavior (it lived only in the seed, now fixed).
- **Dispatch discipline that bit this session:** a plain `forge next`/`forge invoke` Bash call gets SIGTERM-capped at 2 minutes and orphans the task in `running` with a dead container. Run EVERY multi-minute forge command under `forge launch run --require-control-toolchain --name <n> -- <cmd>` and observe via a condition-driven Monitor on durable state. To recover a `running`-but-dead pipeline task: `forge cancel <task>` (→ failed) then `forge retry <task>` then `forge next` — `recover --continue` REFUSES a pipeline task (host-side finalize never ran).

**Decisions worth not relitigating:**
- **OQ-2 resolved (host evidence):** with `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1` the disposable Monitor wrapping `forge launch wait` is the ONLY supported wake channel; Monitor RETAINED as the named transport (not retired); ScheduleWakeup demoted to a fixed health-bound watchdog. Do not reopen.
- **Controller-identity model (operator decision):** code-enforced, fail-closed, `CLAUDE_CODE_SESSION_ID`-only for the session level (speculative aliases removed — they could merge distinct controllers). Same session re-adopts; a replacement session cannot renew a live lease, takes over only after expiry. FG-562 primitives were NOT modified — fencing is consumer-side via unique owners.
- **Dispatch-key index:** UNIQUE partial index on `json_extract(metadata,'$.dispatchKey')` (NULL receipts non-unique) enforces one-run-per-receipt at the DB level; created behind a `PRAGMA table_info(runs)` metadata guard via a single `.immediate()` atomic, duplicate-guarded convergence (main is born-unique; a stale dev DB converges without bricking). The round-2 non-atomic convergence was the wrong shape; this is the right one.
- **Pre-existing seed↔CLAUDE.md Merge-auth drift** (FG-495 propagation gap) was reconciled inside FG-563 so the new seed↔block parity test (`orchestrator-block-parity.test.ts`, uses the real `applyOrchestratorBlock`) holds. Re-render the block via `npx tsx` of the installer render (project-only, no `~/.forge` write) rather than full `forge upgrade`, which would install unmerged seeds machine-wide.
- FG-563 took 4 review-driven fix rounds, ALL clustered in the concurrency/identity/receipt area — FG-564 touches the same primitives; brief the reds on same-owner fencing, receipt uniqueness, record-before-advance ordering, and real-path (not fixture) proof from the start.

**Shipped (for reference):** FG-563 (`9a3235d`, PR #143) — `forge continue` full-identity continuation-consumer (BD-3 authoritative read, exactly-once claim, F17 adopt-not-duplicate), `forge lost-signals` audit, UNIQUE dispatch_key receipt bridge, fail-closed controller identity, completion-driven orchestrator policy (seed + CLAUDE.md, BD-9 contradiction removed) with seed↔block parity test.
