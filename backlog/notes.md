**Overnight autonomous run ended 2026-07-04 ~01:30 PDT.** Long, high-throughput run — shipped all FOUR core reliability/campaign priorities cleanly (FG-455 pieces 2&3, FG-441, FG-437, FG-434), each engineer→red-review→fix→host-verify→docs→PR→merge. Every merge gated on a green host `npm run test:all` (final suite 2884/2884 root + 27/27 dashboard) + `npm run typecheck`. Full decision journal (11 decisions) in `notes/overnight-decisions-2026-07-03.md`.

**Shipped / merged this run:**
- **FG-455 pieces 2 & 3 — PR #13 (merge 27ab422). Ticket STAYS OPEN (piece 4 remains).** Fanout-parent reconcile + fanout-safe `forge cancel --abandon-run` (kills child containers, no silent run-abandon); new `forge recover <id>` (inspect / `--continue` adopt-and-complete via `markTaskRecovered` / `--re-drive` fanout wave); `forge retry` refuses a fanout child/parent without `--force`; `forge show` surfaces `fanout_wave_orphaned`. Red-wide: 2 HIGH fixed. Docs: concepts.md + FORGE-DEC-024.
- **FG-441 — PR #14 (54d4495), CLOSED.** `forge campaign resume` now reconciles a manually-driven `awaiting_gate` item from durable FG-428 evidence (events-aware) → ships or safe-refuses (durable `evidence_reconcile_refused` event), releasing held dependents. New running-gated CAS `updateCampaignItemIfCampaignRunning`. Red-wide verified 8 invariants; 2 in-scope fixed.
- **FG-437 — PR #15 (eb57b0f), CLOSED.** reconcile recovers a task crashed mid dependency-provisioning (new `container.provision_started/_succeeded` events) — defers a live provisioner, reaps a confirmed-gone one, fails the task retryable. Red-wide: 9 invariants, 3 non-HIGH fixed. **Premise correction:** the ticket named `forge-provision-<taskId>`; the real container is `forge-provision-<cacheKey>` — implementation reaps the real one.
- **FG-434 — PR #16 (db35669), CLOSED.** `forge dependency-cache prune` for the shared `forge-deps-*` volumes + markers (in-use skip, marker-consistency safety, `--dry-run`/`--json`).

**Filed this run (open follow-ups):**
- **FG-457** — `forge review-loop` reports `reviewer_failed` when the reviewer actually returned a valid `fail` verdict (bookkeeping glitch hit during FG-455 review; the real findings were only in the task result.json). Infra bug.
- **FG-458** — campaign reconcile cross-lane consistency: `forge campaign reconcile` (FG-443 out-of-band, events-blind) + report.ts hint could ship an `awaiting_gate`+runId item whose run has an unresolved authoritative FAIL that `resume` (FG-441) correctly refuses. Pre-existing seam FG-441 surfaced.

**Picked up next (recommended order):**
1. **FG-450** — Dashboard "Forge Fleet in Motion" marquee stats band (the remaining priority-5; FG-449 is already done/superseded by it). Deliberately NOT started overnight (D11): frontend + subjective "catchy" tone + needs browser-tools visual verification — better with a fresh context and your eye. Fully scoped in the ticket (4-6 Forge-native cards from local SQLite).
2. **FG-455 piece 4** — the two ACs left open: (a) OOM/SIGKILL/exit-137 classification (defaultContainerAlive never inspects ExitCode/OOMKilled); (b) Mode A — a detached `forge invoke` that finishes but leaves the run falsely `complete` with an empty result.json (reconcile only revisits `running` tasks, never backfills a `complete` task's empty result from the container's own /task/result.json). Documented on the FG-455 ticket body.
3. Then the pre-existing backlog (FG-436/439 orchestrator review-gate autonomy, FG-453/458 campaign-reconcile alignment, etc.).

**Flags / state to remember:**
- **campaign-922c83b7c577 preserved** — still `paused`, untouched by all FG work this run (verified). Do NOT resume/reconcile/mutate.
- **Ops incident still open (deliberately left, D3):** `retry_orphan` — `task-build-a1b968` (pending) stranded under the abandoned FG-442 campaign run `run-campaign-execution-lanes-fg-442-f7ca3f`. Harmless (never dispatches). `forge ops repair task-build-a1b968` clears it if you want tidy; not blocking.
- Dispatch-reliability workaround held all night (detached `nohup ... & disown` + separate PID observer) — no wrapper-kill losses across ~10 long invokes. FG-455 (the dispatch-reliability ticket) is what's closing this gap for real.
- Host-local `notes/overnight-decisions-2026-07-03.md` (uncommitted, on disk) holds the full decision journal; `backlog/notes.md` is this handoff. Both deliberately kept out of the source PRs (FG-380).

**Addendum (post-handoff review fixes, PR #17 / 9117d2d merged):** Two reviewer findings on the merged work, both fixed: (F1, medium) `forge recover --re-drive` now refuses a fanout parent unless its failure kind is `fanout_wave_orphaned` (was re-driving any failed-kind parent) — negative test asserts no primary minted; (F2, low) the short-provisioner `dependency_provisioning_failed` event now logs the real `forge-provision-<cacheKey>` name, not the stale `<taskId>` one. See D12 in the decision journal.
