**Last session ended 2026-07-12 (evening).**

**Where we left off:** Operator-directed autonomous queue FG-539 → FG-540 → FG-425 → FG-527: 2 shipped+closed, 1 paused on an operator decision, 1 deliberately not started (operator stopped the queue to assemble a fresh overnight queue separately — do NOT auto-start FG-527).

**Queue outcomes:**
1. **FG-539 SHIPPED+CLOSED** — review-loop range inference recognizes standard `(FG-xxx)` subjects. PR #112, merge `739b0b2`. 2 loop runs; round-caught fixes: subject-only matching (body cross-contamination), hyphen-continuation boundary. Docs: not needed (grammar was never documented).
2. **FG-540 SHIPPED+CLOSED** — Codex structured-result recovery from a cleanly-completed stream. PR #113, merge `0a2ce3e`. ONE shared extraction rule (src/v2/stream-result-recovery.ts) at 5 consumers (invoke, runNext, reconcile running-orphan, Mode-A backfill, recover --continue); operator-decided intactness rule; final-turn envelope + alternation; malformed result.json refuses ALL stdout recovery; persist-before-complete everywhere; provenance event task.result_recovered_from_stream (6 sources); E2E incident-replay tests. 10 loop runs. **Merged on operator authority** over one re-found, operator-dispositioned finding (reviewer-schema-at-recovery = accepted parity; evidence in PR #113 body). Reviewer-demanded stricter implementation preserved UNMERGED on branch `fg540-recover-schema-validation-evidence`. Docs: updated (maintainer run-fg-540-docs-reconcile-71ddac); residue: FORGE-DEC-024's two stale precedence lines (deliberate non-edit — ADRs amend by supersession, orchestrator call pending).
3. **FG-425 PAUSED — operator decision needed (process-supervision boundary).** Branch `fix/fg425-project-gate-locking` pushed at `ce22024` (11 commits), ticket OPEN, NOT merged. Core shipped-on-branch and proven: per-projectDir lock over merge→gate→finalize at all 4 runNext sites (acquireFileLockBlocking, canonicalized key, finalize inside window per loop finding), live-holder-waited/dead-pid-stolen, operator waiting lines, cross-process regression (fails on pre-lock code, 10/10 stressed), different projects parallel, Linux CI platform fix, gate process-group ownership (detached spawn, pending sidecar BEFORE spawn, timeout TERM→grace→KILL→confirmed, fail-closed unconfirmable), docs reconciled twice (maintainer runs ec5a1c + 26addd — NOTE: concepts.md now ahead of code decision, re-check after the decision). 4 loop runs; in-loop finds all fixed until the identity rabbit hole: lstart-precision → command-nonce identity (ce22024) → round-2 zombie-leader edge (ps renders zombies without command → nonce check misreads reuse → enters over a live descendant group). **Recommended resolution (awaiting operator): strictly fail-closed reaping — reap ONLY on affirmative identity match; ALL ambiguity (absent/zombie/unverifiable/mismatch) blocks with the actionable kill -9 error; delete the auto-enter-on-reuse branch.** Rare manual unblock beats wrong-kill and wrong-enter. The deeper alternative is real process supervision = the strategic review §3.1 daemon.
4. **FG-527 NOT STARTED** (queue stopped by operator).

**Also this session:** FG-542 closed (931d6e3, operator-directed). FG-543 filed (agent-image STALE release-check false positive — mtime-vs-ctime; rebuilds can never clear it). FG-548 filed (store deferred-write-txn SQLITE_BUSY under multi-process WAL — surfaced by FG-425's cross-process harness; retry shim in the test cites it). PR #111 merged (forge claude bakes CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1). Dashboard restarted under tmux (launch-dashboard-fstupk). Agent image rebuilt (ignore the STALE flag — FG-543).

**FG-396 prerequisite status (asked explicitly):** NOT yet cleared — FG-425's exclusion core is implemented, reviewed through 4 loop runs, and pushed, but unmerged pending the fail-closed decision. Once FG-425 merges, the integration-lock prerequisite for FG-396 parallel lanes is cleared (FG-410 already closed).

**Next-queue candidates (grounded in the reviews + this session):**
- FG-425 completion (the fail-closed simplification is a small deletion + test flips once decided) — unblocks FG-396 shaping.
- FG-548 (store WAL immediate-write-txn) — correctness, multi-process, prerequisite-adjacent to parallel lanes.
- FG-547 (duplicate push+PR CI) and FG-541 (review-loop CI visibility on just-pushed shas — hit ~8 more times today via local fallbacks).
- NOT yet ticketed, worth filing at queue assembly: (a) review-loop disposition carry-forward — reviewers re-find operator-dispositioned findings (cost FG-540 ~3 loop runs); (b) dashboard in-flight blindness to host-side loop phases (CI-wait/verify have no run rows).

**Process lessons recorded:** launch-record `running` describes the wrapper process, not the work — read run rows/agent feeds first. `forge backlog file` prints the assigned id — READ it (FG-544 body was briefly overwritten by assuming FG-544; restored from git). Concurrent sessions + loop fixers editing the same tree: dedupe carefully, never assume your edit was the only one.

**Decisions worth not relitigating:** FG-540 reviewer-schema parity disposition (PR #113 body is the record). The 3-loop-run stop condition and operator-merge-authority path both worked as designed. FG-535/536-era wait rules unchanged (ScheduleWakeup owns delays; forge launch owns work).
