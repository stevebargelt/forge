**Last session ended 2026-07-08 (autonomous campaign, operator-directed).**

**Where we left off:** Campaign campaign-baddfd198ba3 COMPLETE — 4/4 shipped in order: FG-474 (PR #65), FG-495 (PR #66), FG-493 (PR #67), FG-435 (PR #68). All merged through the new dual required CI checks, all closed with merge evidence, host_verifications rows 15–18 all source='ci' (zero reconcile host-suite executions). Journal: notes/autonomous-decisions-2026-07-07c.md D1–D25 (host-local).

**The friction-reduction objective landed:**
- CI (`.github/workflows/ci.yml`) is the deterministic gate: job `test` (fast canonical gate, 33–41s in CI / ~8.5s host) + job `test-extended` (integration+worktree+dashboard-integration, ~4.5m, parallel). BOTH are required branch-protection contexts on main (`["test","test-extended"]` — context = job NAME, not the "CI / test" display form; display form silently never matches, hit live).
- Suite tiering (FG-495): test:all went 107.6s → ~2.5s steady-state. npm test = unit tier. test:extended owns slow coverage. Tier purity content-guard extended to dashboard.
- Evidence reuse (FG-474): findCoveringGateEvidence — review-loop verification and campaign reconcile consume a passing host_verifications row or whole-workflow-green CI at the exact sha instead of re-running. CI evidence requires per-project content-verified pairing (projectCiRunsCommand) AND every job of the project's CI workflow green at the sha (a green fast job with red/pending sibling never covers). Fail-closed everywhere.
- Review-loop (FG-493): anchor-less red-schema findings coerce to unanchored instead of false reviewer_failed (root cause validated against the preserved FG-489 artifact — it was the finding schema, NOT verdict vocabulary, which was already mapped).
- forge claude (FG-435): STS/SSO preflight fully profile-scoped; `aws configure export-credentials` is authoritative (stale mtime or expired-SSO downgrade to advisory when it succeeds; hard block only when it also fails, naming the profile); non-SSO static-credential profiles exempt from the SSO gate; timezone-labeled expiry evidence.

**Picked up next (operator's call):**
1. ~~WORK-LAPTOP SMOKE TEST for FG-435~~ DONE 2026-07-08: operator ran `forge claude` on the work laptop with NO flags — auto mode-selection picked bedrock from env, preflight resolved the profile, launch clean, no false block. That machine is where the 2026-07-07 false-block recurrences happened, so the regression scenario is confirmed fixed in production. (Positive-detection case — catching a GENUINELY stale cache — remains unexercised; fails open by design, no action unless a real staleness slips through someday.)
2. FG-497 (filed this session): review-loop reviewer dispatch crashes at exec ('argument list too long') on large packets (>~128KB inline) — deterministic on big ranges; forced manual red fallbacks 3× tonight. Fix shape: file-mount the packet. Evidence preserved in run-review-loop-fg-474-fd2c31 and -fg-495-96162a.
3. test-extended flake watch: 'stale-plan check: mutating a DIFFERENT directory does not affect the hash check' failed push-event run / passed PR-event run at same sha (160b556), once. File only on recurrence (journal D21).
4. FG-487 (host-side work visibility) — much of tonight's host-side wait moved to visible CI checks, but review-loop rounds remain dashboard-invisible; re-scope before starting (operator scheduling call, unchanged).

**External state to remember:** Branch protection on main now REQUIRES both checks — direct pushes by admin still bypass (enforce_admins off; backlog-only pushes keep working). Orchestrator policy updated (seed + CLAUDE.md): NO host test:all before merge ever — gate on the PR's two CI checks; review-loop reuses CI evidence at the same sha (worked live all night). Prior stuck run run-fg-425-e1dd27 still wants `forge ops repair` (operator call, unchanged). Ops-check orphan warnings on task-engineer-67e458/6609d6/0fd451 + FG-491 rows remain PRESERVED EVIDENCE — do not clean up. FG-491 gained 5+ new false-positive occurrences tonight (watchdog can't see self-committed work — every one verified on disk per FG-377 rule before any re-run).

**Decisions worth not relitigating:**
- FG-495 budgets stayed separate from FG-474 per operator directive; extended tier BLOCKS merge (continue-on-error rejected — 'do not weaken trust-sensitive coverage' was decisive).
- CI evidence semantics: whole-workflow green at exact sha, per-project content-verified pairing. Two HIGH spoofing vectors (command-agnostic CI coverage; pairing anchored to forge's own repo) were caught by reds and fixed — don't loosen.
- FG-493's root cause is the finding schema, not verdict vocabulary; MVP contract note added to FG-301 recap (dated, history preserved).
- needs_fix_max_rounds during this campaign was continued (not escalated) per the operator's no-stop directive — findings were mechanical; disposition journaled each time.
- Deferred residue lives in journal D20/D21/D23/D24 — deliberately unfiled per backlog-growth discipline; don't refile without new evidence.

**Post-campaign addendum (same session):** FG-435 work-laptop validation surfaced BOTH detector halves working (advisory fired, auth proceeded natively) → operator directed advisory-only interactive preflight → FG-499 filed, implemented, and SHIPPED (PR #69, 3c75f9e): forge claude never hard-blocks on credentials (diagnostics kept as warnings; tests assert launch-proceeds); container-dispatch gate (validateCredsForNewRun) deliberately unchanged + pinned by regression test; quick-start reconciled; FG-435 done-record carries a dated supersession note.

**Shipped (for reference):** FG-474 (PR #65, 7ae74c2 — CI required check + evidence reuse; 10 commits). FG-495 (PR #66, 63f342d — tiering, 16 commits, canonical gate 107.6s→8.5s host / 33-41s CI). FG-493 (PR #67, bf01768 — finding-schema coercion; first natural loop-closeable). FG-435 (PR #68, 9e2b307 — profile-scoped auth preflight; 7 commits). Filed: FG-495 + FG-499 (both shipped same session), FG-497 (open). User-filed mid-session: FG-496, FG-498.
