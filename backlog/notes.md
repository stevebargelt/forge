**Last session ended 2026-07-03.** Big session — shipped TWO stories (FG-452, FG-442) plus filed/enriched the dispatch-reliability ticket FG-455.

**Shipped this session:**
- **FG-452** — out-of-band CODE-TOUCHING campaign lane brought to reconcile parity (capture + ancestry/base-reachability matching + operator surface). Closed the FG-443×FG-440 seam that wedged FG-422 in campaign-922. PR #10, merge e50941e. (3 review rounds: found the AC5 top-level-next-action gap + docs drift the automated reviewers missed.)
- **FG-442** — campaign execution LANES: each item routes into an explicit policy-derived lane instead of silently defaulting to full feature. PR #11, merge 6a435f3. 8 commits, 20 files.

**FG-442 was a haul — read this before touching campaign dispatch/approve:**
- The feature PIPELINE run got ABANDONED mid-build by the dispatch-kill issue (see FG-455 + memory). Recovered by re-executing the (already-designed+planned) build as a DIRECT detached `forge invoke`, then replicating the pipeline's rigor manually (review-loop fell back to manual red-wide; test-engineer role satisfied by the engineer's own tests).
- Review journey: 2 red-wide escalation passes (found+fixed 3 HIGH: resume-bypass of the lane_escalation pause, unwired escalate flow + approve rubber-stamp, legacy-invoke mis-dispatch) → USER's own review found 3 MORE (HIGH: invoke lanes marked complete without shipping; MEDIUM: paused-approve could rewrite metadata on preserved campaigns like 922; MEDIUM: campaign plan still silently defaulted to full_feature) → focused re-review (pass) → store trust-gate hardening (atomic+symmetric approveCampaign CAS).
- Key shipped behaviors to remember: invoke lanes (quick_implementation/docs_only/etc.) only reach lifecycleStatus:complete when the ticket is actually closed (status done + closedCommit); otherwise they PARK awaiting_gate + pause (slots into the FG-443/452 out-of-band reconcile shape-2). `campaign approve` on a paused campaign requires a fresh escalate-produced plan_hash AND no unresolved lane_escalation (enforced in CLI + store) — this protects campaign-922 from metadata rewrite. `campaign plan` REFUSES a silent full_feature default: supply --routes (orchestrator/routing judgment) or explicit --default-lane <lane> --default-lane-rationale <text>. No autonomous classifier agent was added (operator decision: Option B — that's separate routing scope). lane_escalation is a new SHARED blocker; clear it only via `campaign escalate-lane` -> approve -> resume.

**Filed this session (open follow-ups):**
- **FG-455** — agent dispatch reliability: killed wrapper leaves orphaned task with persisted work and no result. Enriched with forensics: TWO modes (direct invoke detaches+survives but writes empty result.json on a falsely-complete run; pipeline `forge next` runs ATTACHED and dies with the wrapper -> orphaned partial work). ~10-12min duration threshold. Cancelling a fanoutParent ABANDONS the whole run. Candidate fix: read the container's own /task/result.json as authoritative.
- **FG-453** — align done-audit host-verification aggregation (any-fail-wins) with reconcile's passing-row model (or document stricter audit semantics). Includes fail-then-pass/fail-only AC + 2 red-wide low findings to absorb.
- **FG-454** — trust-model docs note: ancestry+base-reachability coverage widens a single manual record-host-verification row's scope within a ticket.

**DISPATCH-RELIABILITY WORKAROUND (critical until FG-455 is fixed):** long agent invokes (>~10min) get killed under a tracked harness background. Working pattern: launch `forge invoke ... &` via `nohup ... </dev/null & disown` in an INSTANT-RETURN foreground Bash call (harness never tracks the child), then a SEPARATE observer-only background waiter (`for i in $(seq 1 27); do kill -0 $PID || break; sleep 20; done`) that only watches the pid — killing the waiter can't touch the detached invoke. Do NOT combine launch+waiter in one background command (the kill takes the child too). Do NOT use the feature pipeline for long builds in this harness (attached `forge next` dies). Costs real overhead per long call.

**Picked up next (Phase 6 continuation):**
1. **FG-441** — resume reconciles manually-driven campaign item runs after merge/close. Remaining bounded wiring in the resume path (executor.ts ~542): before driveWorkflowItem on an awaiting_gate item, run the out-of-band evidence check + reconcile-to-complete. ALL primitives now exist (FG-443 evidence, FG-440 capture, FG-427 per-task eval, FG-452 out-of-band capture+ancestry+surface, FG-442 lane dispatch). Note the FG-442 change: invoke-lane items now legitimately reach awaiting_gate on unshipped completion, which is exactly the shape FG-441 should reconcile.
2. Then FG-439/FG-436 (orchestrator/campaign review-gate autonomy — default routine reviews to bounded review-loop), or FG-450 (dashboard "fleet in motion" morale win).

**External state:** campaign-922c83b7c577 STILL paused/preserved — do NOT clean up; FG-442's paused-approve guard now actively protects it from accidental metadata rewrite. main clean at the FG-442 close bookkeeping commit.
