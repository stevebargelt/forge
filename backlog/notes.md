**Last updated 2026-07-12 (early morning). The operator's 4-item autonomous queue is COMPLETE (4/4).**

**Shipped this session, in order:**
1. **FG-531** (awaiting_red crash window) — killed fixer's work recovered byte-identical to the preserved snapshot, review-loop closeable, PR #107 merged (670ebaf), closed.
2. **FG-533** (pre-container crash window) — reconcile's pre-container sweep (retryable `pre_container_crash`, five guards preserving invoke/manual/fanout-parent/live-container/live-lock exemptions), last FG-530 known-failure pin deleted, ops read-surface sees both pre-container shapes, event-lost completions route through container-gone finalization. PR #108 merged (9487474), closed.
3. **FG-535** (durable launcher) — NEW `forge launch run/list/show/rm`: tmux-owned detached execution for long commands; persists command/launcherPid/ownerPid/start/log/OS-verdict exit record (WIFSIGNALED separate from code — exit 143 alone is NEVER attribution); statuses signaled/terminated_unattributed/owner_gone/unknown all evidence-bounded; orchestrator guidance forbids run_in_background ownership (seed + CLAUDE.md re-rendered). Validated with a 34-min tmux-owned review-loop. Attribution ACs resolved by operator direction (si_pid capture sufficient; A/B matrix not run). PR #109 merged (af57d25), closed.
4. **FG-536** (docker-detached agent execution) — containers survive ANY host-side parent death by construction: `docker run -d` + disposable logs/wait watchers, stdin via /task mount entry script (FG-497 bound intact), container ID in start evidence (recorded only after run -d succeeds), reconcile-side surviving idle bound, provisioner + FORGE_DETACHED_EXEC=off stay attached. FORGE-DEC-025 ADR. Live-proven: SIGKILL of the CLI and SIGTERM of its whole subtree (the FG-535 kill signature) both left containers running; reconcile finalized real results. Cause-claim in the ticket corrected per operator direction (sender proven, trigger unestablished). PR #110 merged (07cb63e), closed.

**Standing operational facts:**
- CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1 is armed in the orchestrator session; `forge launch` is the mandatory owner for long commands (guidance in the rendered orchestrator block).
- With FG-536 live (forge runs from linked source), agent containers are kill-proof by construction — the FG-535 launcher remains the operational owner for the forge CLI process itself (review-loops, test suites).
- ~/.forge/launches/ holds the session's launch records incl. the FG-536 kill-smoke evidence (kill2 = SIGKILL, kill3 = SIGTERM-tree); tmux sessions freed, records kept. ~/.forge/sigterm-probe/ unchanged (sentinel + smoking-gun log + FG-531 fixer snapshot).
- Local `main` in THIS worktree cannot be checked out (locked codex worktree /private/tmp/forge-codex-fg537 holds it, another session's FG-537/FG-538 research); backlog closes were pushed via branches off origin/main (chore/fg533-close, chore/fg535-close, chore/fg536-close — safe to delete once the codex worktrees release main).
- Review-loop friction worth tickets next session: (a) a just-pushed sha reads "CI unavailable" (no check-runs registered yet) → local fallback instead of the FG-501 wait path; (b) one transient reviewer_failed (invalid/absent reviewer result) consumed a whole loop run; (c) `forge review-loop <ticket>` range inference failed for FG-533/FG-535/FG-536 ("no commits reference") despite ticket ids in commit subjects — --since worked every time.

**Not to relitigate:** FORGE-DEC-024/FG-479 stands — reconcile never inlines finalizePrimary; pipeline recovery is re-drive through the real path. FG-523's validation contract is a pipeline implementer-step gate, NOT an invoke gate (docs/ADR now say exactly this). The dashboard restart (pre-FG-521 server) is still pending at operator convenience.
