**Last session ended 2026-07-14 (overnight, autonomous).**

**Where we left off:** **FG-425 is MERGED (`b228af9`, PR #116) and CLOSED.** The operator handed the whole chain over to run unattended and it landed: engineer fix -> host verification -> docs verification -> commit/push -> required CI -> bounded review-loop (three passes) -> one corrective fixer for a real defect the loop caught -> merge -> close. `main` is at `2ecc598` (the backlog close).

**What shipped in FG-425 beyond the previous session's AC1-AC4 work:**
- **Defect A (campaign) — `forge campaign resume` can now actually converge a lost publication window.** `driveWorkflowItem`'s `awaiting_recovery` park used to RETURN before Step 5's `runNext` call — and `runNext` is the only caller of the AD-5 convergence authority (`recoverUnfinishedPublications` / `reconcilePublicationRecoveries`). Every resume re-parked identically (a closed loop) while the park's own `requestedHumanAction` told the operator to run that exact command. It now calls through the existing authority BEFORE deciding to park, bounded by `CONVERGE_LIMIT = 2`. No second recovery implementation in the campaign layer; nothing is re-dispatched.
- **Defect B (campaign) — a shipped item no longer keeps a stale shared `git_state` blocker.** `reconcileTerminalOutcome`'s ship branch now clears `blockerKind` + `requestedHumanAction` at the centralized terminal write, the moment shipment is durable. An item whose publication is still UNRESOLVED keeps its blocker and still pauses the campaign — that is intentional and must not be "simplified" later.
- **Remote AD-5 recovery defect (found by the review-loop, NOT by the campaign proof).** Recovery read the target ref, fetched, then classified against the sha read BEFORE the fetch — so a remote move inside that window could TERMINALIZE an attempt from a snapshot the fetch had already disproved (an AC5 violation: a terminal disposition over a still-`publishing` attempt). `recoverCheckout` now re-resolves the tip after the fetch (`rev-parse FETCH_HEAD`) and classifies against THAT; `candidateInTargetHistory` returns the answer together with the tip it was answered against. Unanswerable -> `RemoteAncestryUnknownError`, nothing settled, attempt stays `publishing`, next sweep re-reads. Three regression tests, each proven red against the prior tip.

**The campaign proof is now committed:** `src/campaign/fg425-campaign-lost-window.worktree.test.ts` — four production-path tests (real `runNext`, real publisher, real durable rows). Three were red against the pre-fix tip. Do not weaken them.

**Picked up next:**

1. **FG-551** (tmux missing from the agent image) — still the highest-leverage small ticket. 10 FG-535 launch tests hard-fail in EVERY agent container, so every agent's suite looks red and every session re-diagnoses it. It cost diagnosis time again this session.
2. **FG-555** (filed this session) — `forge launch run` gives launched commands a LOGIN shell whose node (v23) mismatches the repo's better-sqlite3 native build (v24). Every DB-touching test in a launched suite dies with `ERR_DLOPEN_FAILED` and the whole run looks red. This burned a full verification cycle tonight. Workaround in use: prepend `/Users/stevebargelt/.nvm/versions/node/v24.17.0/bin` to PATH inside the launched command.
3. **FG-556** — `fg425-publication-cas.worktree.test.ts` asserts an un-canonicalized `/var` path; fails on macOS hosts only (`/var` -> `/private/var` symlink), green in Linux CI. PROVEN pre-existing (fails identically on a clean tip). It will keep making host runs look red until fixed.
4. **FG-557** — `fg520-forge-test-resync` "no-change re-sync should be fast" is a wall-clock threshold; 17s under host load vs sub-second unloaded. Load-flaky.
5. **FG-558** — the accepted dashboard PRD (`docs/prds/dashboard-original.md:19`) omits `awaiting_recovery` from the task-status icon contract. Surfaced by the review-loop; the fixer's scope guard correctly refused it as out-of-range.

**Decisions worth not relitigating (carried forward, all still binding):**
- The publisher architecture is SETTLED: the lane provides ORDERING and spans candidate integration + full validation + publication; the short mutex + CAS + ancestry proof + immutable `candidateSha` binding provide CORRECTNESS; the lane is a renewable durable LEASE — no PID probing, signalling, identity nonces, zombie classification, or reaping, ever; validation stays INSIDE the lane turn and OUTSIDE the mutex window; publication worktrees stay fresh/create-only/per-attempt; publisher state stays under FORGE_HOME. Three separate reds have now tried to move validation out of the lane turn and were wrong every time.
- **A terminal refusal may NEVER stand over an attempt still recorded `publishing`.** This is the invariant that caught the remote-recovery defect. It is not cosmetic and not a messaging bug.
- **`MutexLostMidPublishError` is REACHABLE in production** (suspend, SIGSTOP, paused container, swap thrash, long GC/IO stall). Do not try to close anything by arguing unreachability.
- **A cancel is terminal and wins.** Recovery may never resurrect a cancelled task — but the operator must still be told when a cancelled task's candidate DID land.
- **Recovery may fetch into the LOCAL object store; it may never mutate the target.** No push, no update-ref on the remote, no force, no lease consumption.

**Operational notes from the overnight run:**
- The review-loop's own fixer introduced a second-order bug in its first fix (classifying from a stale pre-fetch sha), which the loop's NEXT round caught. Reading the loop's fixer commits before pushing them is not optional.
- `forge backlog file` reassigns a sticky number if an existing ticket file is temporarily moved out of `backlog/` (e.g. stashed to give `review-loop` a clean tree). This briefly collided with FG-554 tonight; caught and repaired. Never `forge backlog file` while a ticket file is stashed aside.
- Two branches still deliberately unmerged: `fix/fg425-project-gate-locking` (`ce22024`, superseded) and `fg540-recover-schema-validation-evidence`.
