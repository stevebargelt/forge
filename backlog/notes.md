**Session 2026-07-18 (autonomous FG-561 campaign, operator away).** GO: take FG-555 → FG-552 → FG-562 → FG-563 end-to-end, sequentially, without waiting. Recovery governor in force.

**FG-555 — SHIPPED + CLOSED (merged `21af80c` / PR #138, closed `1af8474`).** Honest R3/R4 launched-workload provenance + the `--require-control-toolchain` control-toolchain contract. Two operator decisions on record (both to note in any final report):
- **Decision 1 — contract shape = Option A (narrow pinned-PATH trust):** a control tool resolved BY NAME on the pinned control PATH (forge/npm/npx) or a node/nodejs whose ABI matches is ALLOWED; login shells, `env PATH=`/bare `VAR=VAL` mutation, explicit-path wrong-ABI interpreters, and other shells/scripts/wrappers are REFUSED before execute. Probe-only. R4 defaults to `unknowable`.
- **Decision 2 — merge authorized despite review-loop non-`closeable`:** red-wide kept re-flagging the Option-A name-resolution trust (`launch.ts:714` — a name-resolved forge/npm could be a wrapper). That is the operator-accepted residual (compromised-host non-threat, honestly recorded R4=unknowable, documented) — NOT a wrong-ship. Merged on green CI (both `test` + `test-extended`). Took 7 review-loop passes; every real bug fixed; all 7 ACs walked with evidence before close.

**IN PROGRESS — FG-552 (Slice 2, launch-wait primitive).** Branch `fg552-launch-wait-primitive` (off `21af80c`). Engineer dispatched: launch `launch-fg552-eng-4525s3`, run title "FG-552 launch wait primitive + atomic records", route implementation_quick (cohesive new-primitive → one coherent change avoids fanout collisions; PRD Slice 2 settles the design). Brief carries: atomic exit+meta records (BD-4, temp+rename), reader-honesty (empty≠terminal, bounded retry), subscribe-race close (BD-6), minimal observer + `src/cli/index.ts` eager-import refactor (F33), the `forge launch wait <id>` blocking command semantics, mandatory reconciliation for owner_gone/unknown (F34), the full F4–F11/F32–F34 + degraded-tmux observed-RED matrix, and PROPOSED OQ-4 (waiter cancel = `wait_cancelled`, never touches the tmux work, distinct audit) + OQ-5 (post-reboot `unknown` default, refine only on independent durable evidence, never fabricate an exit record) resolutions for review. After engineer: test-engineer → review-loop → both CI checks → merge → close → docs. NEXT after FG-552: FG-562 (durable continuation claim — includes an operator-AUTHORIZED schema/migration + `docs/SCHEMA-CONTRACT.md` obligation) → FG-563 (interactive orchestrator adoption — do NOT auto-edit memories; propose one-line corrections per the ticket).

**Dependency check (done, per GO):** FG-581/582/583 are FG-572 promotion/installed-surface concerns; promotion not activated on this host; NONE blocks the continuation chain. FG-553 Slice-1 (BD-14 R1/R2) already landed (FG-567/568/569/570/571).

**External state:**
- **`~/code/forge-agent-work` = writable clone**, on `fg552-launch-wait-primitive`. Live control checkout `~/code/forge` on `main` at `1af8474`, clean. Sync clone before reusing; branch fresh from main per ticket.
- **INTERIM dispatch override (until 552/562/563 land):** every orchestrator-owned agent / multi-minute forge command via `forge launch run`; record launch ID, arm a condition-driven Monitor observer; timed wakeup only as lost-observer backstop. Bash timeout/143 ≠ permission to re-dispatch — inspect durable launch/run/task/container evidence first. A Forge-owned verification/test chain that must run under forge's own toolchain adds `--require-control-toolchain` (FG-555, now shipped).
- **Branch protection on `main`:** BOTH `test` + `test-extended` required. Clean review-loop (or explicit operator merge authorization) + both green authorizes the orchestrator to merge. Backlog/notes commits go straight to main. NOTE: do NOT commit `backlog/notes.md` edits into a feature branch — it collides with the handoff commit on main (hit live on FG-555 PR #138, resolved by aligning the branch to main).
- **Agent git constraints:** briefs forbid checkout/switch/reset/pull/rebase unless assigned; verify branch/HEAD after each agent returns. Agents self-commit (Steven Bargelt authored) — verify on disk + push.
- **Retained evidence/planning refs** (don't delete as stale without disposition): foundations integration/lanes, `origin/fg540-recover-schema-validation-evidence`, `origin/fix/fg425-project-gate-locking`, local `backlog/fg554-graphify`.
- **Do NOT mutate/reconcile meatgeekv2 state.**

**Review-loop reality (learned on FG-555):** an operator-decided residual that a stateless adversarial reviewer will always re-flag makes the review-loop structurally unable to emit `closeable`. When ALL real findings are fixed and the only remainder is an operator-decided-and-documented residual, that is an operator merge-authorization decision — surface it, don't loop forever.
