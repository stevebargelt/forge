**Last session ended 2026-07-18.**

**Where we left off:** Operator-directed overnight autonomous run took FG-579 then FG-585 end-to-end (implement → RED-proof → bounded review → CI → merge → close → docs). Both shipped. The meatgeekv2 synchronous-invoke incident was added to FG-563 as a live F22 reproduction (`5f7e515`): FG-536 preserved the detached agent, but the control chain stopped and asked the operator to “give me a nudge.” Last exchange: operator confirmed NOT to file the optional finalize-CAS generation-predicate hardening. Nothing Forge-owned is mid-flight; both Forge checkouts are clean at origin/main (`3b9a999`) before this handoff update.

**RECOVERY GOVERNOR still in force (operator, 2026-07-17):** FG-561 automatic advancement remains PAUSED. No new ticket/AC from a finding unless it falsifies an accepted AC or shows credible in-scope data-loss/security harm; optional hardening deferred by default; product properties, schema, required CI gates need explicit operator authorization; campaign health = net open-backlog reduction. Do NOT dispatch agents/pipelines/review-loops or start implementation children without an explicit operator go. This session's go covered ONLY FG-579 + FG-585.

**Picked up next (NONE auto-starts — all gated on explicit operator go):**
1. **The primary product-critical path is now FG-555 → FG-552 → FG-562 → FG-563.** This closes launched-workload provenance, supplies `forge launch wait`, adds the phase-bound durable continuation claim, then makes the interactive orchestrator advance without operator nudges. The operator considers the current kickoff/continuation gap crippling; prioritize this chain over FG-518/FG-527/FG-590 and unrelated backlog.
2. **Before delaying that chain for every open FG-572 promotion child, perform ONE narrow dependency check only:** determine whether FG-581/582/583 actually block the observer/continuation path. The load-bearing stable-runtime pieces (FG-569/570/571) have landed. If the remaining installed-surface work is independent, record that fact and proceed; do not start another broad architecture audit.
3. **FG-581/582/583 remain valid but deferred** unless that narrow check proves a specific one blocks FG-555/552. Promotion is still not activated on this host.
4. **FG-590 remains real but later.** Until it lands, close every autonomous session with explicit terminal-launch/tmux cleanup and an evidence-safe container sweep.
5. **FG-518 and FG-527 remain active but are not the next priority.** FG-518 owns the generic campaign resume message for workflow drift; FG-527 is a later FG-477 lifecycle slice.

**External state to remember:**
- **`~/code/forge-agent-work` is the writable implementation clone** — clean on `main` at 3b9a999. Reuse by branching fresh from main; sync first.
- **Shared-clone git hazard (live this session):** a container agent's own `git pull/checkout/reset` on the shared clone switched it off the feature branch once while another session moved origin/main. No work was lost. During an active orchestrator run, this Codex/session must NOT push unrelated commits directly to main; queue them until the run stops. Agent briefs must forbid checkout/switch/reset/pull/rebase unless that exact operation is assigned. Keep the feature branch durable on origin and verify branch/head after each agent returns.
- **Both checkouts synced to origin/main (3b9a999).** The live control checkout `~/code/forge` is current (the earlier "behind at 5f7e515" is resolved).
- **INTERIM dispatch override until FG-552/562/563 land:** every orchestrator-owned agent or potentially multi-minute Forge command (`invoke`, `new`, `next`, `review-loop`, long verification) goes through `forge launch run`; never guess that an invoke will be short. A Forge-owned verification/test chain that must run under forge's own toolchain adds `--require-control-toolchain` (FG-555) so an ambient login-shell PATH cannot select an ABI-incompatible node and produce a wall of false-red failures. Record the launch ID, arm a condition-driven observer immediately, and use a timed wakeup only as a lost-observer backstop. A Bash timeout/143 is NOT permission to re-dispatch: inspect durable launch/run/task/container evidence. Never end with “give me a nudge” or ask the operator to say “check.”
- **Cross-project boundary after reboot:** global `~/.forge` still showed meatgeekv2 run `run-mg-19-fix-ci-pipeline-b154ec` active: original task `task-engineer-a97d77` recorded running although its container exited 0 and its result file is populated; replacement task `task-engineer-fd999c` is complete; terminal launch `launch-mg19-cifix2-cou1w2` and the stopped container remain. These belong to meatgeekv2, NOT Forge. Let the meatgeekv2 orchestrator reconcile, ingest, terminalize, and clean them; do not mutate them from a Forge session.
- **Branch/worktree state:** both checked-out Forge working trees are on `main`, clean and synced; no extra worktree is registered. Intentionally retained evidence/planning refs include the foundations integration/lanes, `origin/fg540-recover-schema-validation-evidence`, `origin/fix/fg425-project-gate-locking`, and local `backlog/fg554-graphify`; do not delete them as “stale” without establishing their disposition.
- **Doctor image warning:** `docker/build.sh` completed successfully and the image directly proved tmux + Node 24 + required CLIs, but dev-mode `forge doctor` still says STALE because the cached image keeps an older creation timestamp than a later Dockerfile mtime (FG-543 heuristic). Do not rebuild repeatedly after reboot unless image contents actually changed.
- **Branch protection on `main` requires BOTH `test` and `test-extended`.** The extended tier (integration shards + worktree + dashboard) caught what the fast tier + review-loop rounds missed this session — do not treat a green fast `test` as sufficient.
- **Optional finalize-CAS generation-predicate hardening: operator DECIDED NOT to file (2026-07-18).** The per-run lock (next/gate/retry all hold withRunLock) + fresh reclassification prevents the stale-finalizer schedule. Revisit ONLY if a real lock-bypass path appears (a finalize site not under withRunLock, or a non-CLI concurrent finalizer).

**Decisions worth not relitigating:**
- **FG-585 = Option A: `failed` is the 4th RunStatus** (complete=successful, failed=failed/unreachable required phase, abandoned=cancel). Shipped as an FG-477 read-side slice, one shared classifier, NO DB migration (runs.status is unconstrained TEXT). Don't re-debate the vocabulary.
- **FG-530 invariant-3 "cancel race" was a STALE TEST, not a prod bug.** FG-585's terminal-crossing guard correctly refuses cancelling an already-`complete` run (matches performCancel's terminal refusal); the test modeled an impossible transition and was corrected (test-only, AWN-2 coverage retained + strengthened with explicit arms). Don't reopen it as a regression.
- **FG-585 campaign resume-path message degradation → belongs to existing FG-518,** not re-fixed in FG-585 (the safety invariant holds there: it parks/refuses, never mis-runs; only the message is generic).
- **FG-579 `skills` seed-drift coverage = report-only prose** (installed to ~/.claude/skills, compared via CLAUDE_SKILLS_DIR), not a dispatch refusal. In scope, shipped.

**Shipped (for reference):**
- **FG-579** (merged 1aa25a0, closed bdb7cff) — named refusal at workflow dispatch when an installed forge-owned workflow drifts from the release seed bytes; two-axis seed-drift (ownership/coupling); workflows + report-only skills coverage; shared SHA-256 byte identity.
- **FG-585** (merged f0661fa, closed 3b9a999) — `failed` RunStatus so a run with a failed/unreachable required phase no longer false-reports complete; shared classifier across all finalize sites; full consumer sweep; terminal-crossing wrong-ship guard; durable docs reconciled.
