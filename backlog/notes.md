**Last session ended 2026-07-13.**

**Where we left off:** FG-425 was NOT merged. Its process-supervision design (PGID sidecars, identity nonces, PID-reuse detection, zombie classification, orphan reaping) was **replaced wholesale** by a **serialized integration publisher**, and seven binding architecture decisions (AD-1..AD-7) were recorded in the ticket. The operator then tightened the publication contract further (commit `1590a08`). No implementation was started — the session stopped on the operator's explicit stop condition. FG-548 and FG-549 were filed; project memory was triaged.

**Picked up next:**
1. **FG-425 — architecture pass, THEN implementation. Do not skip to code.** Read the ticket body whole: it carries the 10-step design, AD-1..AD-7 (binding), the salvage list, and the acceptance criteria. Three contract points the operator added personally and that a naive implementation WILL get wrong: (a) a remote lease push requires **proving `candidateSha` descends from `baseSha` first** — `--force-with-lease` is only the atomic stale-base guard and is NEVER authorization for a non-fast-forward rewrite; (b) **FIFO must be mechanically testable**, not "we take a lock and hope" — durable enqueue-order key assigned at request-record time, order preserved across forge processes, queued/holding attempt operator-visible, and an abandoned/terminal entry must be skippable without permanently wedging the lane; (c) a `learnings/decisions/` **ADR is now an acceptance criterion**, not a nicety — it must land with the implementation and explicitly supersede the abandoned design.
2. **FG-549** — `ops check`'s `orphaned_work_may_persist` never clears (`detectOrphanedWorkMayPersist`, src/ops/detect.ts:247, JOINs `runs` but never filters `r.status`). This is the source of the 12 permanent high-severity incidents on this project. **Polarity trap:** sibling detectors (detect.ts:69, :105) filter on `TERMINAL_RUN_STATES` because a terminal run MAKES their incident; here a terminal run makes it STALE — the fix is the INVERSE filter, and pattern-matching the neighbours gets it backwards. Ticket carries three candidate policies; pick one and state the reasoning in code. Small, isolated, good standalone ticket.
3. **FG-548** — store deferred-write-txn SQLITE_BUSY under multi-process WAL (deferred snapshot upgrade bypasses `busy_timeout`). Correctness; independent of the FG-425 redesign.
4. Non-ticket thread — two items from the 2026-07-12 queue session still unfiled: (a) review-loop **disposition carry-forward** (reviewers re-find operator-dispositioned findings; cost FG-540 ~3 loop runs); (b) dashboard **in-flight blindness to host-side loop phases** (CI-wait/verify have no run/task rows). File these at next queue assembly. Related and already filed: FG-541, FG-547.

**External state to remember:**
- Branch `fix/fg425-project-gate-locking` (`ce22024`, 11 commits, pushed): **DELIBERATELY UNMERGED AND ABANDONED — do not merge, do not delete.** SALVAGE: canonical project identity (`projectIntegrationLockKey`, realpath-canonicalized — the key AD-2's lane and the publication lock are both keyed on), contention visibility (`describeWait`), and the cross-process publication test harness. DISCARD: long-gate locking + the entire process-supervision layer (`GateGroupRecord`, `leaderCommandOf`/`LeaderProbe`, `terminateProcessGroup`, `reapResidualGateGroup`, the leader nonce in `integration-gate.ts`).
- Branch `fg540-recover-schema-validation-evidence` on origin — deliberately UNMERGED (referenced from PR #113's disposition). Do not delete.
- `run-fg-425-e1dd27` (the obsolete original-design run) was marked **abandoned** via `forge ops repair` on operator authority. **Do NOT retry any task from it.**
- `forge ops check` still reports 12 `orphaned_work_may_persist` incidents — all shared-project-dir, tree clean, nothing actually stranded. This is the FG-549 bug, not real work. Ignore until FG-549 lands.
- Dashboard runs tmux-owned (launch-dashboard-fstupk, port 8024). Agent-image release-check STALE flag is a known false positive (FG-543) — ignore until fixed.
- Project memory was triaged: 8 files' frontmatter normalized to the nested `metadata.type` schema, 1 duplicate archived to `memory/archive/`, and **4 memories whose headlines had inverted against current policy were rewritten** (forge-on-forge now DOES route through agents; NEVER background a forge command — launch it under tmux; code = branch+PR+required CI while backlog-only = direct-to-main; CI is the full-suite gate, not a host re-run).

**Decisions worth not relitigating:**
- **FG-425's process-supervision design is SUPERSEDED, not paused.** The strict fail-closed reaping rules approved earlier that same session were overtaken by the architecture correction and were never implemented. **Do NOT reintroduce pre-merge orphan reaping as a safety mechanism (AD-7).** The load-bearing reason: supervision was only ever needed because the gate ran against the publish target. Off the target — and with publication bound to the recorded immutable `candidateSha` (AD-6) — an orphaned gate group is a resource leak, not a correctness hazard. Cleanup is GC (FG-356), never a correctness gate.
- **AD-1 × AD-2 interaction — do not mis-tune.** With the FIFO lane, Forge-owned attempts cannot move each other's base, so `publish_base_churn` fires essentially only on an EXTERNAL writer (operator pushing to the target mid-run). Repeated churn parks are a signal about external write traffic, NOT forge contention. Do not respond by raising the AD-1 bound (two full validations: initial + ONE rebuild).
- **AD-2 does not conflict with FG-396.** The FIFO lane orders *publication*, not *execution* — parallel campaign lanes stay parallel through worker execution and serialize only at integration/publish.
- FG-548's direct-to-main backlog filing is accepted — do not redo it through a PR.
- FG-540 reviewer-schema-at-recovery = accepted parity, merged on operator authority; the record is PR #113's body.
- FORGE-DEC-024's two stale precedence lines: deliberate non-edit (ADRs amend by supersession).
- Process lessons: `forge backlog file` prints the assigned id — READ it. Launch-record `running` = the wrapper, not the work. **Concurrent sessions edit the same tree — never assume sole authorship** (the operator pushed `1590a08` mid-session; it was folded in, not overwritten).

**Shipped (for reference):** No code shipped this session — it was a design-correction session. Backlog/design commits: `67f5be0` (FG-425 rescoped to serialized integration publisher; FG-548 filed on main) · `97aebe7` (AD-1..AD-7 recorded) · `2d80335` (FG-549 filed) · `1590a08` (operator-authored: ancestry-proof-before-lease, FIFO mechanically testable, ADR promoted to an AC). Prior sessions: FG-539 (PR #112 / `739b0b2`) · FG-540 (PR #113 / `0a2ce3e`) · FG-542 (`931d6e3`, PR #111).
