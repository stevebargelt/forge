**Last session ended 2026-07-10 (overnight reliability queue, operator-directed).**

**Where we left off:** The bounded overnight queue from the 2026-07-09 engineering audit is COMPLETE — 4/4 items merged and closed, each through the full per-item discipline (narrow plan → engineer + invariant tests → CI green on the exact PR head → review-loop → merge on normal closeable evidence → AC-walk close). **No PR #87-style exception was used anywhere** — every merge rode reviewer-pass + trusted-tip + both required CI checks green.

**Shipped and closed this session:**
- FG-508 part 1 (earlier, same day): codex CLI pin 0.135.0 → 0.144.1 (PR #88, merge b03a3f5); codex-subscription smoke verified before/after. Ticket remains OPEN for AC2/AC3 (loop survives a reviewer model_error: same-round retry on default profile or documented pinning + regression test). The old "review-loops can die structurally at any round" workaround guidance is OBSOLETE — the codex provider path works again.
- FG-509 (audit N-A): drive-loop throw containment — doGate + reconcileTerminalOutcome wrapped in parkCampaignOnDriveThrow; recoverable park + running→paused + resume re-drive, incl. the terminal-reconcile shape. PR #89, merge 9e3a920.
- FG-510 (audit N-D): ship-gate coverage spoof closed — resolveGateCoverage requires command === gate (one covering-row definition with findCoveringGateEvidence); the legacy done-audit test that codified the spoof was inverted. PR #90, merge 363128b.
- FG-511 (audit N-B): evidence-gated campaign retry for campaign_system items — fail-closed probe (linked non-complete run, failed primaries ALL classifying auth/infrastructure), distinct campaign_item.campaign_system_retried event atomic with the CAS reset, and review-caught hardening: SHIP EVIDENCE OUTRANKS TRANSIENT EVIDENCE (delivered work refuses retry, routes to reconcile; verb + show/report gate on one shared composition). PR #91, merge e9c981b.
- FG-507: recover/retry lifecycle gaps — recover recommends the verbatim-working `forge cancel && forge retry` sequence (recommendationCommands in --json); retried ad-hoc tasks re-dispatch directly via the shared dispatchInvokeTask with a recorded-facts fail-closed plan; taskPackage.dispatchSource:"invoke" provenance ends discriminator inference (RetryDispatchKindUnknownError: workflow_unloadable | legacy_ambiguous_phase, pre-write, never strands a row). PR #92, merge 6120940.

**FG-507 settledness decision (operator-decided, do not relitigate):** isRunSettled returns false for any NONTERMINAL top-level task with dispatchSource:"invoke" — run-level live work, visible to settledness but EXCLUDED from step-state and ready-queue projection; run lock NOT held across ad-hoc container dispatch; reconcile unchanged (already sweeps nonterminal tasks); FG-477/FG-512 deliberately not absorbed. Accepted failure mode (documented in concepts.md): a pending ad-hoc row stranded by a crash between insert and dispatch holds the run open until `forge cancel`. Production-CLI coverage exists: fg507-recover-retry-cli.integration.test.ts drives the real recover (human + --json) → cancel → retry commander handlers with only docker shimmed.

**Injectable-clock CI flake fix (rode PR #92):** dashboard queries-verification tests seeded fixtures against hardcoded NOW=2026-07-09T12:00:00Z while reviewLoopRunPhases read real Date.now() for its 24h STALE_LOOKBACK_MS — a time bomb that armed 2026-07-10 and reddened the required `CI / test` check on EVERY branch. reviewLoopRunPhases now takes nowMs (injectable, defaulting to Date.now(); /api endpoint passes it explicitly). If CI mysteriously reddens on untouched code again, suspect this class first.

**Picked up next:**
1. FG-512 — runner-side dispatch provenance (stamp dispatchSource:"workflow" at runNext/gate insertTask sites, then delete taskDispatchKind rule (d)). ACTIVE and INTENTIONALLY SEPARATE from FG-507 — do not fold into other work. Residual corner until then: a genuine workflow step literally named "task" is unretryable (no shipped workflow has one; grep-verified).
2. FG-508 remaining AC2/AC3 — reviewer model_error resilience (why did a loop round rotate reviewer profiles claude→codex? pin per-loop or same-round retry on default) + regression test.
3. Campaign cleanup (non-ticket thread, operator go still pending): campaign-7a56519b2f3d items stuck failed/campaign_system — NOW recoverable two ways: `forge campaign reconcile` (FG-502 shape) for delivered items, or the new FG-511 evidence-gated `forge campaign retry` for transient never-finished items. Natural first dogfood of both paths.
4. Normal backlog order: FG-402/FG-395 (dashboard attention inbox / campaign view), FG-425 stuck run (run-fg-425-e1dd27 still wants `forge ops repair` — operator call), FG-496 DB-backed backlog, FG-506 (remaining half: bespoke scope_guard_revert_failed stop message).

**External state:** ops-check still shows the 7 pre-existing orphaned_work_may_persist rows (FG-455/475/479/485/501/502 evidence; tickets closed — cleanable whenever) + the FG-425 stuck run. Branch protection unchanged (test + test-extended required). Review-loop trails for this session: ~/.forge/runs/run-review-loop-fg-509-*/-fg-510-*/-fg-511-*/-fg-507-* notes.

**Decisions worth not relitigating (this session):**
- Review dispositions recorded in the TICKET BODY reach subsequent loop reviewers (used successfully on FG-510: done-ticket prose is historical record, never rewritten to track later semantics; extended-tier proof is the required CI check, not an in-container run).
- Loop cadence that worked: fix real findings via engineer invoke on the same run → push (fixer commits are LOCAL-ONLY and force an extra confirmation pass if left unpushed — preempt known nits before looping) → fresh loop. Reviewers earned their rounds on 3 of 4 items.
- Engineer container discipline: /project/node_modules is an empty volume; agents npm-install ONLY in the /tmp/forge-work scratch copy; forge-test scratch does NOT resync src/ on reuse (stale-source hazard) and tier flags override file args.
- Detached review-loops via python subprocess start_new_session=True + Monitor on process exit remains the reliable overnight pattern (harness kills backgrounded Bash forge processes).
