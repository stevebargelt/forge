**Last session ended 2026-07-30.**

**Where we left off:** The operator's authorized queue ran to completion autonomously: FG-608 (live DB cutover) → FG-645 → FG-642 → the evidence-led program FG-638 → FG-639 → FG-640 (+ FG-541 superseded) → FG-647, every ticket merged on green ten-check CI and closed on an AC evidence walk. The evidence-led review lifecycle is FULLY LIVE: `feature` declares `review_mode: evidence_led` (active generation gen-tzoggznmyy), Change 0 is retired from the policy blocks, `forge review-loop` is deprecated, and FG-647 was reviewed end-to-end by `forge review` itself — the first production `settled` review, including a real fix cycle and the eight-check shipping review with per-test-validated AC evidence.

**Picked up next:**

1. **FG-649 — coordinator candidate re-anchoring after post-hoc fix commits.** The sharpest live gap: it bit twice (the FG-639 pilot and FG-647's first recheck bound to a pre-fix candidate). The fix→docs-rebind→recheck ordering routes around it, but every future review pays that tax until fixed. Folds in: `continue` resolving the dispatch project from CWD instead of the persisted workspace, and fix-batch payloads including already-resolved findings.
2. **FG-650 — validator tolerance for honest extra keys.** Hit three times live (discovery twice, recheck once); reviewers author correct outcomes with legacy keys appended and the strict root schema refuses them whole. Retry and accept-lens are the designed workarounds; tolerate-and-record or prompt hardening is the fix.
3. **FG-609 (FG-496 Slice D, queue primitives)** is the next PLAN implementation item — NOT yet operator-authorized; ask before dispatching.

**External state to remember:**
- ntfy was DOWN the entire run — every `forge notify` failed `network: fetch failed`; milestones are recorded in the DB but nothing pushed.
- Docker Hub token still presumed unrotated (carried since 2026-07-28).
- Three DB restore points: forge.db.pre-fg608.bak, pre-fg638-ledger.bak (287MB), pre-fg639-coordinator.bak (288MB). All migrations this run were additive; integrity ok; parity guard green.
- FG-646 is explicitly OFF-QUEUE (operator instruction 2026-07-30) — do not work it.
- Two stale orchestrator-session run rows (bf2fa4, 085fdf) remain active in the DB — no liveness signal exists; do NOT cancel by age.

**Decisions worth not relitigating:**
- D5: EXPECTED_TMUX_FAILURES baseline 10→14 accepted on the operator's behalf (the 4 additions are FG-569's tmux-gated tests, verified from the script's own inventory). Veto path: revert 0594424e's script hunk.
- D9: FORGE_CHROME_BIN is authoritative when set — a wrong path errors by name instead of silently using another Chrome.
- D10: findings against frozen backlog/*.md snapshots are resolved by annotating the DB ticket body, never by writing the frozen file.
- D13: accepted_risk authority derives mechanically from the finding row (invariant/acceptance/security/data-integrity markers) — a caller cannot omit it.
- D17: two WRITING agents never share a clone concurrently (a docs-maintainer observed a fixer's uncommitted edits mid-pass); read-only reds may share.
- D18/D19: FG-639's model review closed at three passes with the live pilot as the residual check; the pilot's remaining gap was FILED (FG-649), not driven — reviews of the review engine stop when the findings become another ticket's scope.
- The forge-on-forge migrate predicate concern from the previous handoff did NOT reproduce and stays parked pending a demonstrated failure.

**Shipped (for reference):** FG-608 (`f391b544` machinery + live cutover: 598 tickets, mode db, store banner `store: db`), FG-645 (`1754c386`, 129 in-container reds → 0, harness dirty-tree fix), FG-642 (`0dab8f8d`, browser tier restored + dashboard_browser in required CI), FG-638 (`ea6a9101`, review ledger + disposition CLI), FG-639 (`d118aff` coordinator + `bccec80`/`a6336f5` pilot fixes), FG-640 (`ed9394e`, review_disposition gate + feature migration + Change-0 retirement), FG-541 (superseded on the guarded evidence mapping), FG-647 (`907c899`, zero skips everywhere, first settled production review). Filed: FG-649, FG-650.

Closeout framing corrected (operator, 2026-07-30): the evidence-led lifecycle is live and settled FG-647, but is NOT fully complete — FG-649 (recheck binds to pre-fix candidate after post-hoc fix commits) is a known production stabilization defect and is PROMOTED ahead of FG-648 and FG-609 as a lifecycle correctness blocker. FG-639 stays closed; its ticket body carries the corrected framing.

FG-649 SHIPPED and closed (32fba06, PR #186): the coordinator owns fix-cycle commits (declared-paths-only, named refusals, crash recovery on the same adoption predicate), workspace binds to the review row, batches carry unresolved fix_now only. Review review-4973e90f1e5d settled — first evidence-led review of a pipeline run, driven end-to-end by the new coordinator on its own implementation; review_disposition gate exercised live (refused review_absent, then advanced on the settled ledger). RF-8 deferred to FG-652 (stage-record sha in crash-recovery window, bookkeeping-only). FG-650 fixed and integration-tested (deterministic root cause: harness contract mandates status, lens schemas refused it; tolerate-and-record shipped, unblocked the FG-649 review live) — PR #187 open, CI running; operator decision pending on waiving its evidence-led review; docs impact (operator_behavior_changed) unresolved until then. Pipeline run run-fg-649-...-300e88 parked after the build gate with verify/docs phases undispatched per operator route instruction (review already carried tests+docs); decide complete-vs-cancel later. Leftover: review clones ~/code/forge-fg649 (workspace of the settled review — keep until FG-652 or reap), ~/code/forge-fg650; untracked docs/research/competitive/strands-agents-forge-assessment.md predates this session.

FG-650 SHIPPED and closed (f3ef335 PR #187 + docs d55fe21 PR #188): tolerate-and-record on discovery+recheck root schemas, toleratedRootKeys durably recorded; evidence-led review WAIVED by operator (bounded, integration-tested, production-proven by unblocking the FG-649 review live). Docs impact resolved: updated. Filed FG-653 (docs-maintainer catch: red-wide seed AWN-5 instructs per-finding enrichment keys the still-strict nested DiscoveryFindingSchema refuses — seed-compliant red on a discovery lens refused whole; fix fork is seed carve-out vs schema extension, decide deliberately). Queue heads: FG-652 (RF-8 stage-record sha, small), FG-653, then FG-609 (still NOT operator-authorized). Clones ~/code/forge-fg649 (holds the unsquashed reviewed line the settled ledger's candidate shas point at — do not delete casually) and ~/code/forge-fg650 (reappable) remain.

FG-653 SHIPPED and closed (da349a3, PR #189): five red lens seeds state the discovery-context rule at the AWN-5 section (discovery schema wins; confidence/affected_files/recommended_fix/disposition not emitted; finding_type IS legal; remediation_advice kept), AWN-5 preserved for legacy verdict paths, ledger-only disposition. 24 tests across 3 files; unit 3527/3527, integration shards 4248/4248; negative control proved the AC-5 assertions load-bearing. CI note: dashboard_browser failed once at the Chromium install step ('operation was canceled'); identical-commit re-run passed — infra, not the diff. HOST REPAIR done: installed agent seeds were badly stale — all five red seeds carried NO FG-640 evidence-led section (true cause of the FG-649 malformed_output storm), and  was missing the whole FG-640 Batch-remediation section (explains this session's fixer failures: named-but-unexecuted tests, one empty result.json — the installed seed never had those rules). engineer/shipping-reviewer/documentation-maintainer also refreshed; all provably uncustomized; backups at the session scratchpad forge-agents-backup/. forge-raci.md and prompt-author/templates/README.md correctly left retained (genuine operator content). New seed generation published. FG-654 FILED (HIGH) for the durable ownership/propagation fix — separate Forge-owned protocol from operator customization, deterministic upgrade of the Forge block, refuse evidence-led dispatch on a reviewer lacking the required protocol generation, record protocol generation/hash in task manifest + evidence, cover the five lenses PLUS engineer and shipping-reviewer. Queue: FG-654 (high) > FG-652 > FG-609 (still NOT operator-authorized).
