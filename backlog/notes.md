**Last session ended 2026-07-09 (second session that day).**

**Where we left off:** FG-502 shipped, merged (PR #87), and closed with a full AC walk; the session ended right after closeout. The last open proposal to the operator (not yet approved): run `forge campaign reconcile campaign-7a56519b2f3d` to ship the FG-487/FG-492 items stuck failed/campaign_system — the exact shape FG-502's new recovery handles. That would resolve the long-standing campaign cleanup item properly instead of abandon-or-leave, and is the natural first dogfood of the new reconcile path.

**Picked up next:**
1. Campaign cleanup via the new recovery (non-ticket thread): `forge campaign reconcile campaign-7a56519b2f3d` (campaign must be paused; expect the new campaign_item.campaign_system_reconciled audit events; report/show should preview eligibility first via campaignSystemEligible). Operator had not yet said go.
2. FG-508 — codex reviewer model_error: the agent image's Codex CLI is too old for gpt-5.6-terra; ALSO round-2 of a review-loop resolved the reviewer to codex while round 1 ran claude (why? pin or same-round-retry semantics needed). Until fixed, review-loops can die structurally at any round; workaround is manual review + operator merge authorization (used for PR #87).
3. FG-507 — recover/retry lifecycle gaps (recover recommends a retry that refuses status=running; retried ad-hoc invoke tasks strand pending — forge next won't dispatch them). Hit live twice this session.
4. Normal backlog order thereafter: FG-402/FG-395 (dashboard attention inbox / campaign view), FG-425 gate locking (stuck run run-fg-425-e1dd27 still wants forge ops repair — operator call), FG-496 DB-backed backlog, FG-506 (remaining half: bespoke scope_guard_revert_failed stop message).

**External state to remember:** Ops-check still shows 6 pre-existing orphaned_work_may_persist rows (FG-455/475/479/485/501 evidence, tickets closed — cleanable whenever) + the FG-425 stuck run. Branch protection unchanged (test + test-extended required). Loop logs for this session in scratchpad only (session-scoped, not in notes/) — the durable review trail is in ~/.forge/runs/run-review-loop-fg-502-* notes and the FG-502 run f952cf.

**Decisions worth not relitigating:**
- FG-502 semantics (operator-decided, recorded in the ticket's Non-Goals): backlog/closeout enforcement is PATH-LEVEL for ALL disallowed classes — never whole-round reset; the FG-462 "guard" = classification + always-reverted + closeout-withholding, NOT round blast radius. A reviewer re-reading Non-Goals as whole-round is a known failure mode (loop-1 fixer did it; reversed).
- Reviewed-tip trust: @{u} FIRST, origin/HEAD only as no-upstream fallback (origin/HEAD-first makes feature-branch closeable unreachable — shipped bug, fixed pre-merge); exact sha reachability via merge-base --is-ancestor, never ahead-count; no implicit fetch; no closeable on unresolved/unreachable remote.
- campaign_system recoverable shape = blockerKind campaign_system AND lifecycleStatus failed OR blocked_by_red (executor has FIVE+ producer sites — docs deliberately de-enumerated so a new site can't falsify prose; do not reintroduce counts).
- Gate corrections/clarifications do NOT reach downstream pipeline phases or review-loop runs automatically: inject as run-metadata keys (runNext.ts:414 pours metadata into every task's inputs) for pipeline phases, and record operator decisions in the TICKET body for review-loop reviewers (separate runs never see run metadata). Root cause of the loop-1 reversal; systemic fix unfiled (candidate ticket if it bites again).
- PR #87 merged on operator authorization over a reviewer_failed loop (structural codex error, not a verdict): manual review of the final 58-line delta + 4-loop trail + CI green. Precedent, not a policy change — closeable remains the default gate.
- pipeline blocked_by_red on the FG-502 build: fixed via 2 in-run engineer fix rounds + red re-checks + gate advance --force with documenting rationale (standard recovery; request-changes on fanout still collides).
- Harness kills backgrounded forge processes (~7min observed): dispatch long-running forge invokes/loops via python subprocess start_new_session=True (macOS has no setsid binary; nohup alone insufficient), monitor durable state (result.json / container exit) via Monitor tool, never rely on the Bash background task surviving.
- Engineer npm install in container clobbered host better-sqlite3 ABI again (npm rebuild better-sqlite3 fixes); host suite MUST run under nvm node 24 (bash -lc login shell resolves node 23 → NODE_MODULE_VERSION mismatch, 1443 false failures).

**Shipped (for reference):** FG-502 via PR #87 (merge 5010bb8): path-level fixer scope guard with in-range docs allowance + verify-before-trust revert + whole-closure containment; campaign_system reconcile recovery + distinct audit event + show/report previews + cross-surface parity regression; reviewed-tip trust gate (which refused three local-only tips during its own review cycles — live validation). Docs: concepts/campaign-runner-plan/SCHEMA-CONTRACT/reconcile --help/orchestrator seed + CLAUDE.md re-render. Filed FG-506 (half shipped early via #87), FG-507, FG-508. Backlog closes at f4e8183/d676269.
