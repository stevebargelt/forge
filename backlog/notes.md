**Last session ended 2026-07-18.** ⚠️ **RECOVERY MODE IS STILL ACTIVE — FG-561 automatic advancement remains PAUSED.** The governor below was never lifted; it still governs the next session.

**Where we left off:** Session was CI-performance + housekeeping, all operator-directed and non-ticketed (no backlog tickets opened/closed by this thread). Sharded the required `test-extended` gate 4 ways and added a 6-min ceiling (both merged), pushed two competitive-research docs, and pulled the operator's separately-merged dashboard PR #134 into working git. Nothing mid-flight; nothing auto-resumes.

**RECOVERY GOVERNOR (operator, 2026-07-17 — DO NOT DROP):** No new ticket or AC may be created from a finding unless it directly falsifies an existing accepted acceptance criterion or demonstrates credible in-scope data-loss/security harm. Optional hardening is deferred by default. Product properties, new test environments, dependency policies, and required CI gates require explicit operator authorization. Campaign health is measured by **net open-backlog reduction**. Do not dispatch agents/pipelines/review-loops or start implementation children without an explicit operator go.

**Picked up next (NONE auto-starts — all gated on explicit operator go):**
1. **FG-579 is the next functional campaign blocker** — a CURRENT silent workflow-misrun path (seed-drift omits `workflows`, so a stale installed workflow mis-runs undetected). Must not start without a separate explicit operator go. Completing it must NOT auto-dispatch FG-581/582/583/585.
2. **FG-585 (active, not started)** — a run reports `status: complete` while its gate:auto docs phase silently never ran (false completion). Sibling orchestration-integrity defect to the shipped FG-586; keep separate (operator kept them apart). Fix direction unresolved — decide at start.
3. **Deferred FG-572 promotion-path children:** FG-581 (post-promotion RACI compile only warns), FG-582 (hooks anchoring — T9 = symlink-through-`current`), FG-583 (non-atomic cp-loop install → mixed workflow set). Valid but not current failures (promotion never activated on this host).
4. **Non-ticket thread — optional CI-timeout follow-up:** red-wide's only finding on the sharding PR was LOW/maintainability — the ci.yml regression guard could ALSO assert `push:[main]`, the concurrency block, and per-shard `npm ci`/rebuild so a future edit can't silently drop them. Surfaced, NOT filed (governor defers optional hardening). Operator's call whether to pick it up.

**External state to remember:**
- **`~/code/forge-dashboard` is operator-owned — DO NOT TOUCH.** Branch `dashboard-home`, ~20 files uncommitted (dashboard client/server/queries + `src/util/{github-url,project-meta,projects}`). Operator explicitly CANCELLED any orchestrator action on it; the dashboard work landed separately via **PR #134** (`523e9fd`). That uncommitted tree may now be partly-redundant/divergent vs #134; leave it to the operator.
- **Another orchestrator session is/was ACTIVE concurrently** — it filed FG-590 and committed `backlog/` as `2f39acc` mid-handoff, racing this session's notes write (my first notes commit was silently clobbered and had to be re-applied). Expect concurrent backlog commits; pull before writing backlog.
- **`~/code/forge-fg571` is the standalone writer clone** — used for PRs #132/#133; left on local branch `ci-extended-timeout` (remote merged+deleted). Sync to origin/main and branch fresh before reuse.
- **Operator preference (in force): use Monitor (fires once on terminal state), NOT ScheduleWakeup fixed timers, to wait on launched work.** `forge launch run` owns the work; a Monitor polls durable launch/CI status. Memory `feedback_schedulewakeup_not_sleep_for_waits.md`.
- **When the operator says "commit these N files," take it at face value — don't gate on dup/name checks.** Direct pushback this session for diff-checking two near-identically-named docs first.
- **CI job graph now (`.github/workflows/ci.yml`):** fast `test` (unchanged) + extended gate as SIX concurrent jobs — `integration_1..4` (Node `--test-shard=k/4` via `scripts/run-integration-tests.sh`, each provisioning the F31 ABI-mismatched Node), `worktree`, `dashboard_integration` — behind a fail-closed `if:always()` `test-extended` aggregate (the required branch-protection context). Each of the six carries `timeout-minutes: 6`. Branch protection unchanged (`test`, `test-extended`).
- **Promotion NEVER activated on this host, by design.** `~/.forge/{current,releases,interpreters}` absent; host RACI untouched.

**Decisions worth not relitigating:**
- **CI extended gate: 4-min target MISSED, accepted at ~5 min (operator option A).** Root cause: `src/v2/release.integration.test.ts` — ~200 serial multi-second release-build tests in ONE file, atomic to one shard+worker; native `--test-shard` can't split a file. Beating 4 min needs splitting that file or intra-file concurrency — both test-refactoring gated on explicit operator approval. Do NOT re-attempt without a go.
- **The two CI changes were non-ticketed** (operator directed no backlog item). `scripts/run-integration-tests.sh` is portable to macOS bash 3.2 — two 3.2-only bugs (`mapfile`; empty-array under `set -u`) were caught by HOST validation because the engineer's Linux container (bash 5) can't reproduce them. If you edit that script, validate on the real `/bin/bash` (3.2), not just in-container.
- Prior settled calls still standing: FG-580 offline code KEPT (`b6c6542`); FG-584 & FG-589 WITHDRAWN; FG-577 same-UID `$FORGE_HOME` write = accepted honest limit; FG-586 fail-closed design settled.

**Shipped (for reference — git log is canonical):**
- **CI shard test-extended** (non-ticket) — 4 concurrent integration shards + worktree + dashboard_integration behind a fail-closed aggregate; union == 3388-test baseline exactly; ~9→~5 min (`f218a8c`, PR #132).
- **CI 6-min extended-gate ceiling** (non-ticket) — `timeout-minutes: 6` on all six extended jobs + regression guard; over-6-min ⇒ job cancelled ⇒ aggregate red ⇒ merge blocked (`9c8f8da`, PR #133).
- **Research docs** (non-ticket) — `docs/research/competitive/{agent-deck,agentdeck}-forge-assessment.md` (`3e3b83d`, direct to main).
- **Dashboard home overview + canonical projects** — operator's SEPARATE merge, pulled into working git (`523e9fd`, PR #134). Not orchestrator-authored.
