**Session 2026-07-18/19 (autonomous FG-561 campaign).** GO: FG-555 → FG-552 → FG-562 → FG-563 end-to-end.

**SHIPPED + CLOSED this session (3 of 4):**
- **FG-555** (merged `21af80c`) — R3/R4 launched-workload provenance + `--require-control-toolchain`. Operator decisions: Option A pinned-PATH-trust contract; merge authorized despite review-loop non-closeable. 7 review rounds.
- **FG-552** (merged `2e95b0c`) — blocking `forge launch wait` primitive: atomic records, bounded-retry-to-terminal, no false-completion, F32 windows, launcher-liveness `starting` bound, exhaustive exit-record validation. 11 review rounds. PRD-locked policy: a live tmux owner is NOT terminal evidence.
- **FG-562** (merged `fbb1fc8`) — durable continuation-claim PRIMITIVE (new additive `continuations` table + phase-bound CAS + `adoptOrClaimDispatch`/dispatch_key + `continuationsInDispatch` replay + `continuation_stale_observations` audit; BD-3 claim-only-from-`ready`). **Operator DECISION A: ship primitive-only.** BD-3/F17 are MET *at the primitive*; end-to-end consumer enforcement (authoritative `readLaunch` evidence before observe/claim; receipt-keyed check-before-spawn; crash recovery adopting the original physical run) was **added as binding AC to FG-563 + FG-564** (`db11231`). The primitive RECORDS a caller-supplied observation; it does NOT establish authority — that is the consumer's job. Recorded staged RED→GREEN evidence is in the branch history.

**IN PROGRESS — NEXT (LAST): FG-563 (Slice 4 — interactive orchestrator adoption).** Branch off `fbb1fc8` in `~/code/forge-agent-work`. This is the orchestrator (the interactive Claude Code session) ADOPTING the launch-wait + continuation-claim primitives — replacing Monitor-polling + fixed-estimate wakeups. Scope highlights: F19 (happy-path job outrunning any estimate produces no model wake) + F22 (a routine chain reaching its next decision with the operator sending NO messages) must be observed RED against the current fixed-estimate/Monitor behavior; the FG-562 consumer-enforcement AC (canonical `readLaunch`/`classifyExit` evidence immediately before observe/claim, no fabricated disposition, receipt-keyed check-before-spawn, crash recovery adopts original run); surface (NOT auto-edit) memories this slice's own changes invalidate — propose one-line corrections, operator/operator applies. Recommend an architecture/planning pass first (like FG-562) to scope the orchestrator-adoption + F19/F22 + consumer-enforcement before implementing.

**KNOWN PROCESS ISSUES (this session):**
- **Container agents (`forge invoke engineer`/`documentation-maintainer`) sometimes emit `Co-Authored-By: Claude` trailers.** Squash-merge scrubs `main` (verified clean on 21af80c/2e95b0c/fbb1fc8). Review-loop fixer commits are clean. **Worth a ticket: fix agent container git identity so the trailer is never written.**
- **Do NOT commit `backlog/` edits into a feature branch** (collides with the main handoff commit; hit on FG-555 #138). Backlog/AC edits go on main directly.
- **Salvaging a crashed `forge invoke` (143): watch CI ACTIVELY (duration incl.).** A salvaged self-unvalidated fix hung CI 40+ min once (signal-record regression → a blocking `forge launch wait` test never terminated). Every blocking-wait test must be bounded. CI (both `test` + `test-extended`) is the authority for "unrelated/dirty-tree" failures (fg571-*, launch-r2/FG-569-R2 refuse a release build from a dirty scratch tree — green on clean CI).
- **Review-loop:** `exited_ok` from the launch = PASS/closeable; `exited_error` = any non-pass stop. Fixer commits are local (unpushed) → the loop's verification falls back to local (fast tier) and the dashboard shows "stale · waiting-on-ci" (normal, not a hang). When the reviewer persistently flags something the ticket settles (scope/PRD), disposition/advance-over or escalate — don't oscillate.

**External state:** `~/code/forge-agent-work` = writable clone; live control checkout `~/code/forge` on `main` at `e2ab1bb` clean. INTERIM dispatch override (all multi-minute forge cmds via `forge launch run` + condition-driven Monitor observer). Branch protection: both `test` + `test-extended`. Do NOT mutate meatgeekv2 state. MANY terminal `forge-launch-fg55x/fg56x-*` tmux panes accumulated this session — sweep before ending (FG-590 not landed).
