**Last session ended 2026-07-08.**

**Where we left off:** Operator-directed autonomous campaign (campaign-baddfd198ba3) COMPLETE — 4/4 shipped in order (FG-474, FG-495, FG-493, FG-435; PRs #65–#68) — plus two same-session operator-directed follow-ons: FG-499 (advisory-only forge claude preflight, PR #69) and FG-500 (host-branch gate-list evidence fix, PR #70, operator-reported HIGH iterated twice). Session ended with FG-500 merged/closed and no work in flight. Journal: notes/autonomous-decisions-2026-07-07c.md D1–D28 (host-local, gitignored).

**Picked up next:**
1. FG-497 — review-loop reviewer packet crashes at exec ('argument list too long', >~128KB inline) — now the TOP friction item: it forced manual red fallbacks on three of six closeouts tonight (FG-474, FG-495, FG-500). Fix shape in ticket: file-mount the packet. Evidence preserved in run-review-loop-fg-474-fd2c31 / -fg-495-96162a / -fg-500-910940.
2. FG-494 (ntfy non-Latin-1 title drop) — small quick-lane fix, held over from the previous session's batch list; evidence in ticket.
3. FG-491 (persistence-watchdog false positive) — gained 5+ NEW occurrences tonight, all the same blind spot: the watchdog can't see self-committed agent work (clean tree ⇒ 'work not persisted'). Every occurrence was verified on disk before re-run per the FG-377 rule. Fix is now evidence-rich.
4. FG-487 (dashboard host-side-work visibility) — partially mooted: the deterministic gate moved to visible CI checks, but review-loop rounds are still dashboard-invisible. Re-scope before starting; scheduling remains the operator's call.

**External state to remember:** Branch protection on main REQUIRES both CI checks ("test" + "test-extended" — context strings are the JOB names, never the "CI / test" display form, which silently never matches). enforce_admins off, so operator/orchestrator backlog-only direct pushes still work. Pre-existing stuck run run-fg-425-e1dd27 still wants `forge ops repair` (operator call, unchanged all session). Ops-check orphan warnings (task-engineer-67e458/6609d6/0fd451) + FG-491 rows remain PRESERVED EVIDENCE — do not clean up or retry. One-occurrence flake on watch: 'stale-plan check: mutating a DIFFERENT directory does not affect the hash check' failed push-run/passed PR-run at the same sha (160b556) — file a ticket only on recurrence (journal D21).

**Decisions worth not relitigating:**
- NO host `npm run test:all` before merge, ever — the PR's two CI checks are the deterministic merge gate; review-loop and reconcile REUSE covering evidence (host rows for every member of the derived gate list, or whole-workflow-green CI at the exact sha). Worked live all session; policy surfaces + docs reconciled.
- test-extended BLOCKS merge (continue-on-error was rejected — "do not weaken trust-sensitive coverage" was decisive). Fast tier speed never justifies de-gating integration coverage — the same principle closed the FG-500 host-branch hole.
- CI evidence trust chain: per-project content-verified pairing (projectCiRunsCommand) + every workflow job green + ci rows minted only under that path (which is why a ci-sourced primary row may cover the whole gate list). Two HIGH spoofing vectors were caught and closed en route — don't loosen any of it.
- FG-493's root cause was the loop's finding SCHEMA (anchor-less findings rejected wholesale), NOT verdict vocabulary (already mapped). Genuinely absent results still stop reviewer_failed.
- forge claude never hard-blocks on credentials (FG-499, operator-directed after live validation); the container-dispatch gate (validateCredsForNewRun) deliberately KEEPS blocking and is pinned by a regression test. FG-435's done-record carries a dated supersession note.
- FG-435 work-laptop validation: BOTH detector halves production-proven (advisory fired; auth proceeded natively; flagless launch works via env auto-selection).
- Deferred low-severity residue lives in journal D20/D21/D23/D24 — deliberately unfiled per backlog-growth discipline; don't refile without new evidence.

**Shipped (for reference):** FG-474 (PR #65, 7ae74c2 — CI required check + evidence reuse). FG-495 (PR #66, 63f342d — suite tiering; canonical gate 107.6s→8.5s host / 33–41s CI). FG-493 (PR #67, bf01768 — anchor-less finding coercion; first natural loop-closeable). FG-435 (PR #68, 9e2b307 — profile-scoped STS/SSO preflight). FG-499 (PR #69, 3c75f9e — advisory-only interactive preflight). FG-500 (PR #70, f99e7f4 — host-branch gate-list evidence, shared evaluator). Filed: FG-497 (open), FG-495/FG-499/FG-500 (filed + shipped same session). User-filed: FG-496, FG-498.
