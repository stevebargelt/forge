**Last session: 2026-07-21. FG-565 (Slice 6 closeout) built + PR #151 open — PAUSED on ONE operator decision (Q2 closeout bar). Do NOT merge/close until resolved.**

**Where we are:** FG-565 implementation_full pipeline ran end to end (architect → plan → build fanout → verify → docs) against the writer clone `~/code/forge-agent-work`. Committed to branch `feat/fg565-slice6-closeout`, PR **#151** open. Evidence ledger on main: `docs/plans/fg565-closeout-evidence-ledger.md` (@ `a75bcac`, corrected). Branch tip pushed: `2c5d365`.

**What shipped in the PR (6 proven gaps G1–G6):** G1 new read-only `forge continuation show/list [--state blocked] [--consumer-kind] [--json]` over continuations + continuation_stale_observations (durable-state-only, bounded LIMIT, no N+1, no schema/write); G2 F20 seam test (real tmux ownership + honest reconcile-bound container recovery); G3 F23/F24 broken-source tests (this + unrelated project, hermetic); G4 F21 cancelled-candidate-surfaced assertion driven through the real cancel→campaign_system transition; G5 docs-parity test (quick-start/concepts vs seed, claim-agreement); G6 PRD/docs reconciliation. Verify: 5 integration files, 21/21 green.

**Fix rounds used = 2 of 3 (cap): ≤2 per root cause.** (1) build-fixer for blocked_by_red (F20 was a fake proof, F21 bypassed the real transition, G1 N+1, G6 phantom autonomous-run-prompt.md ref). (2) review-loop round 1 (`2c5d365`): made F20 honest — tmux boundary now `assert.ok(hasTmux)` (fails, never silent-greens); detached-container boundary honestly re-scoped to the surviving-reconcile-bound LOGIC exactly as fg536-idle-bound (documented that no reconcile-family test runs a real Docker container). Reviewer ACCEPTED F20 in round 2 — F20 is resolved and at its 2-round cap.

**THE UNRESOLVED INVARIANT (Q2 — the decision needed):** `forge lost-signals` durably records ONLY watchdog recoveries (`continuation-lost-signal.ts:90` "never called on the normal delivery path"; line 33 notes replay is a "future recovery source ... can be added"). So NORMAL delivery is answerable only by ABSENCE of a recovery row, and REPLAY recovery has NO durable row. Yet multiple surfaces OVERCLAIM a durable normal-vs-watchdog distinction:
- `seeds/orchestrator-template.md:424` AND `CLAUDE.md:512` (marker block): "forge lost-signals shows which transitions were recovered by the watchdog vs delivered normally — durable" — ORCHESTRATOR-POLICY surfaces (mine; pre-existing, predates FG-565; edit needs `forge-dev upgrade`).
- `docs/concepts.md` lost-signals paragraph: "whether by watchdog or normal delivery" (pre-existing).
- `docs/quick-start.md:272`: round-1 already softened this one.
This makes the PRD closeout gate ("Q2 answerable from Forge-owned durable evidence, incl. recovered-by-watchdog/replay") only PARTIALLY met: watchdog=durable, normal=by-absence, replay=unrecorded.

**THE DECISION (operator's — scope/closeout bar, not settled cleanly: ticket says "verify-not-implement / finding against owning slice" → defer; PRD gate says "all questions answerable" → build):**
- **(A) RECOMMENDED — honest-docs + follow-up, close on the verify-and-surface model:** correct the overclaim across concepts.md + seed + CLAUDE.md marker block to state Q2's ACTUAL answerability (watchdog durable; normal by-absence; replay not-yet-recorded); make the G5 parity test assert the delivery-mode semantic; file a follow-up against FG-562/FG-563 for durable normal-delivery + replay-recovery recording (genuinely-new storage scope FG-565's no-new-features fence excludes). Then close FG-565. This is the last (3rd) fix round.
- **(B) pull the durable recording into FG-565** — contradicts its no-new-features fence and expands the epic closeout.

**Blast radius / preserve:** PR #151 + branch `feat/fg565-slice6-closeout` (tip `2c5d365`) preserved on origin. Nothing merged. FG-565 still `story/active`, NOT closed. The clone is on the branch. The round-1 fixer commits are authored as Steven Bargelt (no AI attribution). If resolving via (A) and it touches the seed/CLAUDE.md, remember: edit the SEED then `forge-dev upgrade` (NOT `forge upgrade`), and those policy-surface edits go to main directly, separate from the PR.

**Do next when decided:** resolve per (A) or (B); if (A), that is the final allowed fix round — re-verify with a single red-wide pass, confirm green `test`+`test-extended` on the branch head, merge #151, write the FG-565 AC-evidence grid (persist an `## Acceptance Evidence` section per the AC-evidence policy), close with the merge sha, reconcile the FG-561 campaign closeout (this is the last slice), and update these notes.
