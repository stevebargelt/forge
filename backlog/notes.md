**Last session ended 2026-07-13.**

**Where we left off:** Reopened **FG-425** for targeted corrective changes to the serialized integration publisher (operator-directed, request-changes — NOT a redesign). Five ACs; AC1–AC4 + the laneTick fix are implemented, reviewed, and committed on branch `fix/fg425-publisher-corrective`. **The session stopped mid-flight, by operator instruction, because the final campaign proof exposed TWO REAL PRODUCTION DEFECTS in the AC5 campaign branch.** FG-425 is OPEN and MUST NOT be merged or closed — AC5's campaign clause is unmet.

**Picked up next:**

1. **Decide + fix the two defects the campaign regression exposed** (operator was asked, had not yet answered). Both live inside the AC5 campaign branch shipped in `9ae6278` — they are NOT new scope, and AC5 explicitly requires "the owning task/run/**campaign** reaches the matching truthful state", so FG-425 cannot close with them standing.
   - **Defect A (BLOCKING) — `forge campaign resume` can NEVER converge a lost-window publication; the campaign wedges, and forge tells the operator to run the very command that wedges it.** `src/campaign/executor.ts` `driveWorkflowItem`: the `awaiting_recovery` park (Step 3, ~line 714) RETURNS before Step 5 (~838), and Step 5's `runNext` is the ONLY caller of `recoverUnfinishedPublications` (exhaustive grep). So every resume re-parks identically — a closed loop, zero new events. Meanwhile the item's own `requestedHumanAction` says "Run `forge campaign resume <id>` … to converge the publication (AD-5)". Only `forge next <runId>` / `forge publish recover` actually converge. **This is the FG-475 wedge shape returning.** Suggested fix: an `awaiting_recovery` task must let the drive REACH `runNext` (or invoke convergence directly) — park only AFTER convergence is attempted.
   - **Defect B (HIGH) — a shipped item keeps a stale `git_state` blocker and re-pauses the whole campaign.** The `awaiting_recovery` park is the first campaign park that stamps a `blockerKind` on an item EXPECTED to ship (every other blocker-stamping park is terminal; the gate:human park deliberately stamps none). `reconcileTerminalOutcome`'s ship branch never clears it, and `git_state` is in `SHARED_BLOCKER_KINDS` (`src/campaign/policy.ts:4`), so `driveWorkflowItem` fires `isSharedBlocker` on an already-shipped item and pauses the campaign. Observed even on the WORKING path (`forge next` → resume). Suggested fix: clear `blockerKind` when an item ships; reconsider stamping a SHARED blocker on a non-terminal park expected to continue.

2. **Then finish the operator-approved sequence** (do not deviate): documentation-maintainer VERIFIES (does not expand) the round-1 `docs/concepts.md` correction in `379197f` (brief written, see scratchpad) → push the complete tip → BOTH required CI checks green AT THAT EXACT SHA → ONE bounded final `forge review-loop FG-425 --max-rounds 2 --route implementation_quick --since ea2c026`. **Operator's blocking rule for that final loop: findings block ONLY if they show a HIGH/MEDIUM correctness defect or an unmet AC — everything else becomes a follow-up ticket and must NOT prolong FG-425.** Merge only when the reviewed tip EQUALS the fetched remote head AND the campaign clause has production-path evidence. Close only after walking AC1–AC5 against cited evidence.

3. **FG-551 (tmux missing from the agent image)** — small, high-leverage, and it cost real diagnosis time AGAIN this session: 10 FG-535 launch tests hard-fail in EVERY agent container, so every agent's suite looks red. They pass on the host and in CI. Landing this removes a permanent false-alarm surface.

**External state to remember:**
- Branch `fix/fg425-publisher-corrective` is pushed EXCEPT the tip. **`379197f` is LOCAL-ONLY (unpushed).** CI was green on BOTH required checks (`test`, `test-extended`) at `ba2da75`.
- **UNCOMMITTED and IMPORTANT: `src/campaign/fg425-campaign-lost-window.worktree.test.ts`** (645 lines, untracked) — this is the campaign production-path proof. 4 tests: 1 passes, **3 are RED against the current tip because they expose Defects A and B above.** Do not delete it and do not "fix" it to be green — it is the evidence. Falsification was verified by controlled mutation (neutralising the executor's recovery branch → the park test goes red; executor.ts restored byte-for-byte).
- Untracked `docs/research/competitive/pinecone-forge-assessment.md` is NOT from this work (mtime predates the run, claimed by no agent). Left alone. Never `git add -A` at repo root on this branch.
- Two branches still deliberately unmerged: `fix/fg425-project-gate-locking` (`ce22024`, superseded by FG-425's ADR) and `fg540-recover-schema-validation-evidence`.
- FG-425 added the `publications` table to the host-global `~/.forge/forge.db` (additive, forward-only, migration ran clean). `9ae6278` adds the `awaiting_recovery` value to `tasks.status` — a schema-contract change with an ADR (`learnings/decisions/2026-07-13_awaiting-recovery-task-status.md`).

**Decisions worth not relitigating:**
- **The publisher architecture is SETTLED and was fenced in every brief:** the lane provides ORDERING and SPANS candidate integration + full validation + publication; the short mutex + CAS + fast-forward ancestry proof + immutable `candidateSha` binding provide CORRECTNESS; the lane is a renewable durable LEASE — **no PID probing, signalling, identity nonces, zombie classification, or reaping, ever**; validation stays INSIDE the lane turn and OUTSIDE the mutex window; publication worktrees stay fresh/create-only/per-attempt; publisher state stays under FORGE_HOME. Two separate reds have already tried to move validation out of the lane turn and were WRONG both times. Do not accept it a third time.
- **A terminal refusal may NEVER stand over an attempt still recorded `publishing`** (AC5). The operator OVERRULED an earlier attempt to defer this as a "messaging bug" — it is a contradictory-durable-truth defect (attempt `published`, task `failed`, retry advice inviting a re-run of landed work). Do not re-classify it as cosmetic.
- **`MutexLostMidPublishError` is REACHABLE in production.** "Requires a deschedule past the lease TTL" describes ordinary production (suspend, SIGSTOP, paused container, swap thrash, long GC/IO stall) — it is NOT a proof of impossibility. Do not try to close this by arguing unreachability.
- **A cancel is terminal and wins.** Recovery may never resurrect a cancelled task (`ba2da75`) — but the operator must still be told when a cancelled task's candidate DID land (`task.published_after_cancel`).
- The 10 failing FG-535 tmux tests in agent containers are FG-551, NOT a regression — verified by reproducing them byte-for-byte on the clean tip and by running them green on the host. Don't re-diagnose them from scratch a fourth time.

**Shipped (for reference):**
- **FG-552** (filed) — `forge launch` should PUSH completion events; controllers are currently forced onto fixed-estimate wakeups or back into the harness's SIGTERM-swept background set. Interim: a `Monitor` process polls the launch record / tmux pane and wakes the controller; ScheduleWakeup demoted to a lost-signal watchdog.
- **FG-553** (filed) — an agent mid-editing forge's own source breaks the live CLI **machine-wide** (npm-linked tsx, no build step), including the orchestrator's own observation path. Hit live this session; self-healed. DEC-019's shadow volume fixed the native-binary half of forge-on-forge contamination; the SOURCE half is unsolved.
- FG-425 corrective work committed but NOT merged (see above): `89b475b` AC1/AC2/AC3 · `6ae72f0` laneTick seam + 12 test-engineer tests · `9ae6278` AC5 · `33d2a37` AC4 docs + the `awaiting_recovery` ADR · `ce83310` + `379197f` review-loop round-1 fixes · `ba2da75` cancel-resurrection guard.
