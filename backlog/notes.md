**Last session ended 2026-06-29.**

**Where we left off:** Long Phase-3 session — shipped the readiness→done-audit→shipping-reviewer chain plus a major review-tooling hardening detour (FG-415/FG-417). The user reviewed aggressively and found a real gap after almost every close (FG-413/416/383/415 reopened once each; FG-384 reopened TWICE — a P1 where the shipping-reviewer guardrail was neutralized in the real dispatch path). Last action: re-closed FG-384 after the guardrail P1 fix landed and a confirming red-wide passed clean.

**Picked up next:**
1. **FG-388** — document the Reviewer Context Packet contract (now incl. the `doneAudit` field) + the shipping-reviewer verdict vocabulary/fail-loud precondition. This is FG-384's explicitly deferred docs; the "when FG-384 wires the reviewer reachable" precondition is now met. Closing this ends the FG-384 thread.
2. **Wire shipping-reviewer into a default pipeline workflow (NON-TICKET thread — not yet filed).** The reviewer is fully built, mapped, guardrail-substantiated, and reachable — but DORMANT: no default workflow lists `shipping-reviewer` in its `reds`, so it never runs. Decide whether to add it (which workflow / authority / gate_on_verdict) or leave it opt-in per-project; file a ticket if pursued.
3. **FG-385** (risk-targeted reds planner — remaining Phase-3 quality-gate piece, explicitly out of FG-384 scope) and **FG-386** (dashboard surface for readiness/done-audits).
4. **FG-414** — ops-check should detect active-run/all-terminal-tasks orphans + reconcile `projects show` in-flight count with the dashboard (filed early this session after a phantom "in-flight: 2").
5. **FG-410** — parallel-safe `updateCampaignItem` (low urgency; only needed before FG-396 parallel lanes).

**External state to remember:**
- TWO seed deploys done this session via `FORCE=1 scripts/install-seeds.sh` (the script defaults to `cp -n` no-clobber — FORCE=1 is REQUIRED to update existing installed seeds): the red-wide production-path-trace rubric (FG-417) and the shipping-reviewer rich-verdict contract (FG-384). Both live in `~/.forge/agents/`. install-seeds is machine-wide; only the intended file differed each time.
- No host-verification RECORDER exists (deliberately out of FG-383 scope). Until one ships, done-audit `host_verification` is always `unknown` for real items → done-audit `pass` / campaign `all_shipped` is unreachable for real items, BY DESIGN. Building that recorder (plausibly alongside the shipping reviewer) is what unlocks a real `pass`. (Already bit a test: FG-384's regression couldn't drive doneAudit=pass through the real path, so it tests the accepted_exception non-blocking path instead.)

**Decisions worth not relitigating:**
- **review-loop is SAFE now (FG-415)** — clean-tree precondition, fixer scope-guard (touching `backlog/`/`docs/`/`learnings/`/`README*` → round reverted + `fixer_out_of_scope`), per-round verify-then-commit, dynamic diff (reviewer sees the fixer's committed changes). USE it. TWO exceptions: (a) never review-loop changes to review-loop itself — use manual read-only red-wide (it caught two guard bypasses FG-415's own reviews missed); (b) review-loop REFUSES during a reopen because the uncommitted backlog move makes the tree dirty — use manual red-wide for reopen-fixes (this is the precondition working, not a bug).
- **Reviewer briefs now require a production-path consistency trace (FG-417)** — source→collector→evaluator→state/rerun→operator-surface-AND-JSON→real-input-tests, live in the review-loop reviewTask + red-wide seed. It caught a tautological fixture on FG-384's first dogfood. The session's repeated miss: NEVER close a surface/report/distinguish/gate AC on "the evaluator/JSON supports it" — verify the real data path POPULATES it, the human surface RENDERS it, no step reuses a STALE snapshot, and TEST THROUGH the real path.
- **Shipping-reviewer guardrail substantiation (FG-384 P1)** — forge's red ingestion (runNext.ts:691) downgrades a `fail` with no SURVIVING graded findings to non-blocking `inconclusive`. So `mapShippingReviewerVerdict` must attach a well-formed synthetic finding to EVERY mapper-decided fail UNCONDITIONALLY (the residual was only-when-zero-raw-findings; malformed findings graded to zero still neutralized it). Verdict mapping: ship→pass; ship_with_named_deferrals→pass only when every deferral named+linked; needs_fix→fail; needs_human→inconclusive; guardrail backstop downgrades a ship over a fail/unknown done-audit (no named exception/deferral) to fail. gate.ts UNTOUCHED. (Saved as a memory.)
- **done-audit (FG-383)** is truthful-but-conservative by design: missing required evidence → `unknown` never pass; container_verification is informational/excluded and never satisfies host_verification; deferral_linked needs the follow-up named IN the deferral section, not just any `related[]`.
- **Container env gotcha (reconfirmed repeatedly):** agents run with empty container node_modules and `npm install` in `/tmp` scratch — watch their diffs for stray `package.json`/lockfile tsx version bumps (revert them); "needs npm install / better-sqlite3 rebuild failed" notes are container artifacts. Always verify on the HOST.

**Shipped (for reference):** FG-413 (readiness wired into campaign runner; reopened for operator-surface consistency) · FG-416 (dependency-held resume message operator-loop fix) · FG-383 (mechanical done-audit + report wiring; reopened to collect container evidence) · FG-415 (review-loop fixer scope guard + commit determinism + dynamic diff; reopened for stale-diff + README* guard) · FG-417 (production-path consistency-trace reviewer rubric, both surfaces) · FG-384 (LLM Shipping Reviewer verdict mapping + done-audit packet + guardrail substantiation; reopened twice — operator-surface then the P1). Filed: FG-414. All pushed to origin; host `test:all` green (2335 + 27).
