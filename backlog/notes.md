**Session 2026-07-18 (autonomous FG-561 campaign, operator away).** Operator GO: take FG-555 → FG-552 → FG-562 → FG-563 end-to-end, sequentially, without waiting; stop only on a genuine condition. Recovery governor still in force (defer optional hardening; product properties need explicit operator authorization; no new AC from incidental findings).

**BLOCKED — FG-555 stopped on an OPERATOR CONTRACT-SHAPE DECISION (genuine stop condition).**

- **Dependency check (done, per GO):** FG-581/582/583 are FG-572 promotion / installed-surface concerns (RACI-compile-on-promotion, git-hook symlinks, seed-install atomicity); promotion is not activated on this host. NONE blocks FG-555 or the observer/continuation path. Independent — recorded, proceeded. FG-553 Slice-1 (BD-14 R1/R2 provenance) already landed via FG-567/568/569/570/571, so FG-555's real dep is satisfied.
- **What shipped on branch `fg555-launch-workload-provenance` (PR #138, run `run-fg-555-launched-workload-provenance-abi-refuse-add567`), all pushed, tip `2d3b169`:** honest R3/R4 launched-workload provenance recorded in `workload.json` + surfaced in `forge launch show`/`--json` (mirrors FG-569 R1/R2); exact argv preservation (no synthesized shell); `forge launch run --require-control-toolchain` refuse-before-execute; docs (concepts.md/quick-start §13) + orchestrator seed/CLAUDE.md adoption line reconciled. Integration + unit tests green; every regression observed RED first.
- **THE BLOCKER (review-loop round 5/round-2, `src/v2/launch.ts:659`):** the fail-closed `--require-control-toolchain` allow-list REFUSES the documented primary Forge-owned caller `forge launch run --require-control-toolchain -- forge review-loop …`. It accepts forge/npm/npx only when the executable sits in `dirname(process.execPath)` (the pinned node bin dir), but `forge` is a shim ELSEWHERE that resolves node via its shebang. The tests only simulated forge-in-node-dir, masking it. This is the architecture signal: you cannot simultaneously (a) refuse everything statically unprovable AND (b) allow `forge review-loop`, because forge's runtime resolution is unprovable-but-safe-under-the-pin. 5 review-loop passes (10+ fixer rounds), each surfaced a new facet of the same unwinnable static-pre-verification problem (login shell → abs-path node → env-wrapped shell → npm/script/launcher R4 → shim-not-in-node-dir).
- **RECOMMENDATION for the operator decision (contract shape):** adopt the NARROW/practical contract — the pinned PATH IS the protection for NAME-resolved control tools; ALLOW name-resolved forge/npm/npx/node on the pinned PATH (trust the control-node-first pin), REFUSE only the clearly-unsafe explicit cases (login shell re-sourcing profile, `env PATH=` mutation, explicit-path wrong-ABI interpreter), and RECORD honest R4=unknowable for the rest with docs stating the pinned-PATH guarantee. This is less machinery (governor-aligned), works for the real callers, and is honest. Alternative: keep strict fail-closed and change how Forge-owned verification is launched (direct node/npm form) — needs an operator call on the launch shape.
- **Next authorized move:** operator picks the contract shape → one engineer fix on the branch to implement it → re-run `forge review-loop FG-555 --project ~/code/forge-agent-work --since 3ac2ee5 --route implementation_quick` → on `closeable` + both CI checks green, merge PR #138, close FG-555, then resume FG-552 → FG-562 → FG-563. Do NOT close FG-555 until the primary Forge-owned caller can actually launch under the contract (that AC is currently UNMET).

**Campaign queue (all still gated on FG-555 landing):** FG-552 (launch-wait primitive) → FG-562 (durable continuation claim; includes an authorized schema/migration + docs/SCHEMA-CONTRACT.md obligation) → FG-563 (interactive orchestrator adoption; do NOT auto-edit memories — propose one-line corrections per the ticket).

**External state to remember:**
- **`~/code/forge-agent-work` is the writable implementation clone.** Currently on branch `fg555-launch-workload-provenance` at `2d3b169` (clean, pushed). The live control checkout `~/code/forge` is on `main`, untouched by writers this session. Sync clone before reusing; branch fresh from main for new tickets.
- **INTERIM dispatch override (until FG-552/562/563 land):** every orchestrator-owned agent / multi-minute forge command goes through `forge launch run`; record the launch ID, arm a condition-driven Monitor observer immediately; timed wakeup only as lost-observer backstop. A Bash timeout/143 is NOT permission to re-dispatch — inspect durable launch/run/task/container evidence. A Forge-owned verification/test chain that must run under forge's own toolchain will use `--require-control-toolchain` (FG-555) once its shape is settled.
- **Branch protection on `main` requires BOTH `test` and `test-extended`.** A clean review-loop + both green authorizes the ORCHESTRATOR to merge; backlog/notes commits go straight to main.
- **Agent git constraints:** briefs forbid checkout/switch/reset/pull/rebase unless that exact op is assigned; verify branch/HEAD after each agent returns. Agents committed their own work this session (Steven Bargelt authored commits); that is expected — verify on disk and push.
- **Retained evidence/planning refs** (do not delete as stale without disposition): foundations integration/lanes, `origin/fg540-recover-schema-validation-evidence`, `origin/fix/fg425-project-gate-locking`, local `backlog/fg554-graphify`.
- **Do NOT mutate/reconcile meatgeekv2 state.** Cross-project runs under global `~/.forge` belong to their own orchestrators.
- **This session's launch IDs (all terminal):** engineer `launch-fg555-eng-1mldo9`, test-engineer `launch-fg555-te-39bktt`, docs `launch-fg555-docs-bfc1ux`/`launch-fg555-docs2-orzoar`, fixes `launch-fg555-fix2-ijv969`/`fix3-yykqx3`/`fix4-6jgrpe`/`fix5-jp9mjw`, review-loops `launch-fg555-rl-c6l46s`/`rl2-l0qmvg`/`rl3-d2m4sn`/`rl4-6mp3tp`/`rl5-o0i3ps`. tmux sessions cleaned at session pause.

**Decisions worth not relitigating (this session):**
- FG-555 R3/R4 mirror BD-14's R1/R2 vocabulary; the toolchain guard reuses FG-570's `checkAbi` (extracted to `src/cli/node-abi.ts`), no second ABI checker/version floor.
- The refuse-before-execute + R4 recording were BOTH inverted to a single honest invariant: default to the conservative state (refuse / R4-unknowable), prove the permissive one (allow / not_applicable). The remaining open question is ONLY how permissive the "prove" set is for shim-based control tools — the operator's call above.
