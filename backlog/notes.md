**Last session ended 2026-06-25.**

**Where we left off:** Just shipped FG-382 (mechanical readiness preflight evaluator + `forge readiness` CLI), kicking off Shipping Reviewer / Campaign Phase 3. Long session: deployed FG-407, then built+hardened all of Campaign Runner Phase 1 & 2 (FG-390/391/392/393/394) plus robustness fixes (FG-409/411/412), and updated the orchestrator policy (AC-evidence closing gate + 4 CLAUDE.md findings). User was choosing the next Phase-3 ticket when we stopped.

**Picked up next:**
1. **FG-413** — wire the FG-382 readiness evaluator INTO the campaign runner: populate the campaign report `readiness` field (currently null) and HOLD not-ready items at campaign start/resume (reuse FG-393 hold semantics; needs_refinement/blocked → held, exploratory proceeds). This is the "Option B" follow-up filed this session; the direct consumer of what we just built.
2. **FG-383** — done-audit mechanical closeout checks (the other half of the FG-372 operational-done gate: code+tests+backlog-state+git-state+pushed+deferred-scope before claiming shipped). Pairs with FG-384 (LLM Shipping Reviewer role) and FG-386 (dashboard surface for readiness/done-audits).
3. **FG-410** — parallel-safe `updateCampaignItem` (read-merge-write lost-update); only needed before FG-396 parallel lanes, low urgency.

**External state to remember:**
- The orchestrator-template SEED change (AC-evidence closing gate etc., commit `23bbbe0`) propagates to OTHER forge projects on their next `forge upgrade` — expected, but be aware other projects' CLAUDE.md will shift.
- FG-407 deploy (prior session's open thread) was DONE this session — agent image rebuilt + seeds installed; `forge-test` defaults to the unit tier and seed prose is live.

**Decisions worth not relitigating:**
- **Report-consistency is structural, not per-case.** show/report next-action + safety + the CLI/executor all derive from shared helpers — `campaignBlocker` (default-DENY: only a null blocker yields an actionable suggestion), `findInFlightItem`, `unresolvedBlockedItem`. Any new stop-reason/state flows through these so surfaces cannot drift. FG-394 took 3 reopens learning this; do NOT patch advise/refuse drift case-by-case — fix the shared helper.
- **Campaign terminal transitions use the atomic CAS** `tryTransitionCampaign(id, from, to)` (never check-then-`updateCampaignStatus`, which TOCTOU-throws against a concurrent pause). CAS validated by a host multi-process stress loop (40×24); container cannot reproduce the race.
- **resume = active driver** (paused→running, re-validate approval+hash, skip terminal, drive remaining pending, block). Cooperative pause stops BEFORE the next item. resume/start REFUSE over an in-flight item (`recovery_needed`) — never silently complete a campaign with unfinished work.
- **Blocker/continue policy** (FG-393): SHARED blockers (auth/infra/git/dep/merge/campaign_system) → hold_campaign (paused); LOCAL → dependency-evaluated via ticket `related` (dependent→hold even in pilot; unknown→seq:hold/pilot:continue; independent→continue). Uncategorized/unknown failure → SHARED (FG-412 — never continue past a failure you can't positively call local).
- **Readiness is MECHANICAL** (structural section checks), not an LLM reviewer (epic FG-372 mandate). Operator-instruction reconciliation is explicitly the orchestrator's job, out of the evaluator's reach.
- **AC-evidence closing gate** now in orchestrator policy: before closing a ticket, walk each AC line with concrete evidence; unmet AC → reopen+finish, NEVER close-and-file-a-follow-up for the ticket's own AC. moveTicket now auto-clears stale closed frontmatter on reopen (FG-409).
- HOST `npm run test:all` is the authoritative gate — container integration tests can fail on a scratch-node_modules artifact (ERR_MODULE_NOT_FOUND) while the host is green; always verify on host.

**Shipped (for reference):** FG-407 deploy (non-ticket) · FG-390 campaign data model (`b2ca27b`) · FG-391 planner + plan_hash + `forge campaign plan` (`933ded1`, ADR-023) · FG-392 sequential execution MVP — approve/start, CAS guard (`852cb22` + hardening) · FG-394 operator CLI — show/report/pause/resume/abandon (`62afc29` + several hardening rounds, ADR-024) · FG-393 blocker/continue semantics (`033ada2`) · FG-411 diagnostics resilient to deleted source ticket (`de798d1`) · FG-412 failure-kind conflation / uncategorized→hold (`927a191`) · FG-409 moveTicket clears stale close frontmatter (`f9a…`) · FG-382 readiness preflight + `forge readiness` (`3de2fe5`) · orchestrator policy AC-closing gate (`23bbbe0`). Filed: FG-409/410/411/412/413. Campaign Runner Phase 1 + 2 complete & hardened; Phase 3 started. All pushed to origin (`f9f82c5`); host `test:all` green (2200+27).
