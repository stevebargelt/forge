# forge v2 — `feature-ui-design-needed` workflow translated to YAML
# Source: src/workflows/feature-ui-design-needed.ts
#
# forge's most complex shape today: manual phase (ui-review, no agent — human
# runs Pencil + Claude Code on the host and submits artifacts), gated with
# onReject to brief. Plus the standard architect → plan → build → verify.

name: feature-ui-design-needed
description: |
  Build a feature that needs UI design. brief → ui-review → architect → plan →
  build → verify. The design and implementation share a designDir.

# Model rationale: brief (prompt-author, dense text authoring) + architect
# (systems thinking) stay on spec-writer (Opus). plan / build / verify use
# default (Sonnet) — these are translate-architecture-to-plan, implement-
# against-acceptance, and judge-tests work, well within Sonnet's capability.
# Reds stay on fast-orchestrator (Haiku) for triage.

inputs:
  - name: brief
    required: true
    type: text
    help: "What the feature does AND how the UI should feel. Feeds the design step AND the architect."
  - name: design-dir
    required: true
    type: path
    help: "Where the .pen and designs/ artifacts live."

steps:
  - id: brief
    agent: prompt-author
    model: spec-writer
    gate: human
    workflow_additions: |
      inputs.brief contains the feature + UI brief. Use the ui-design template at
      templates/ui-design.md. Interview the human to fill any missing parameters
      (target_pen_file, output_dir, file_naming, style, screens). Write the
      produced PROMPT.md to /task/PROMPT.md. Output {status, promptPath:
      '/task/PROMPT.md', brief, template: 'ui-design', parameters, openQuestions,
      notes}.

  # Manual phase — no agent runs. Human runs PROMPT.md against Pencil + Claude
  # Code on the host, submits artifacts back via `forge submit`. Schema flags it
  # with `manual: true`. Runner skips agent dispatch entirely.
  - id: ui-review
    manual: true
    depends_on: [brief]
    gate: human
    on_reject: brief

  - id: architect
    agent: architecture-advisor
    model: spec-writer
    depends_on: [ui-review]
    gate: human
    on_reject: brief    # architect reject → redo brief (revise design first)
    workflow_additions: |
      Read the upstream design artifacts: inputs.upstream[*].result.pngFiles
      (canonical Pencil renders). Treat the design as the canonical UI — your
      architecture must support what's drawn. But your output is systems-level:
      risks/constraints/boundaries/priorArt/openQuestions about what supporting
      that design implies for the rest of the system, NOT implementation
      guidance (no type names, no function names).
      Output {risks, constraints, boundaries, priorArt, openQuestions, notes}.
      See architect seed.
    reds:
      - agent: red-wide
        model: fast-orchestrator
        authority: specialist
        gate_on_verdict: false
      - agent: red-narrow
        model: fast-orchestrator
        authority: specialist
        gate_on_verdict: false

  - id: plan
    agent: tech-lead
    model: default
    depends_on: [architect]
    gate: human
    workflow_additions: |
      Translate the architecture document into a step-by-step plan. The plan
      should reference design artifact paths where relevant.
      Output {steps: [{id, summary, files, acceptance}]}.

  # ----- build -----
  # Fanout: one child task per plan-step, agent routed by step.discipline.
  # Reds stay on the parent — they review the aggregate diff once all children
  # settle (per-parent dispatch, not per-child). See #139 for the design call.
  - id: build
    agent: engineer                    # fallback for steps with discipline=general or unmapped
    model: default
    depends_on: [plan]
    gate: verdict     # any authoritative red's fail blocks; human can force-advance with rationale
    workflow_additions: |
      Implement your assigned plan-step (passed via inputs.step). The design PNGs in
      the upstream artifacts are the reference — match the design's spacing, palette,
      and layout where reasonable, using the project's actual stack idioms.
      Output {step_completed, diff_summary, files_modified}.
      The tech-lead asserts your step's files are independent of every other step's files in this build; do not touch files outside your step.files list.
    fanout:
      from_upstream:
        step: plan
        array_key: steps
        input_key: step
      agent_map:
        frontend: frontend-specialist
        backend: backend-specialist
        infosec: security-advisor
        platform: agentic-platform-builder
      max_concurrency: 4
      failure_mode: fail-phase
    reds:
      - agent: red-wide
        model: fast-orchestrator
        authority: authoritative
        gate_on_verdict: true
      - agent: red-narrow
        model: fast-orchestrator
        authority: authoritative
        gate_on_verdict: true
      - agent: red-frontend
        model: fast-orchestrator
        authority: authoritative
        gate_on_verdict: true
      - agent: red-backend
        model: fast-orchestrator
        authority: authoritative
        gate_on_verdict: true
      - agent: red-security
        model: fast-orchestrator
        authority: authoritative
        gate_on_verdict: true

  - id: verify
    agent: test-engineer
    model: default
    depends_on: [build]
    gate: human
    workflow_additions: |
      Write integration and E2E tests for the build. Read the upstream engineer's
      result to understand what changed, then write tests that exercise those
      changes through real user flows and component interactions.
      Output {test_files_written, tests_written, tests_run, tests_passed, tests_failed,
      coverage_summary}.
