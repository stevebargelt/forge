node_modules/
dist/
*.log
.DS_Store
projects/
.overnight-plan-*.md
.claude/

# Debugging artifacts — Playwright traces + ad-hoc dashboard screenshots
.playwright-mcp/
Screenshot*.png
graph-*.png
auth-*.png
sm-*.png
system-map-render*.png
.env

# forge — per-developer claude-code config (machine-local, not committed)
.claude/settings.local.json
.claude/commands/

# forge — transient backlog id-allocation lock (FG-398); never committed
backlog/.backlog-write.lockdir
