# Keep the build context tiny. Everything except the Dockerfile and staged build
# inputs is excluded. build.sh stages corp-root.pem before `docker build`.
*
!agent-dev-worker.Dockerfile
!corp-root.pem
!forge-test.sh
